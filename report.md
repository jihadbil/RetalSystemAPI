# تقرير مراجعة طبقة النماذج وطبقة الوصول إلى البيانات

**المشروع:** RetalSystemAPI  
**تاريخ المراجعة:** 2026-09-11  
**النطاق:** `RetalSystemAPI.Models`، و`RetalSystemAPI.Models.DTOs`، و`RetalSystemAPI.DataAccess`، مع تتبع محدود إلى طبقتي الخدمات وواجهات API للتحقق من أثر المشكلات المكتشفة.

## الملخص التنفيذي

يمتلك المشروع أساساً جيداً: الكيانات منظمة حسب المجالات، وإعدادات EF Core مفصولة في ملفات Fluent API، ويوجد دعم للحذف المنطقي، والعزل متعدد المستأجرين في القراءة، والتدقيق، والمنافسة المتفائلة باستخدام `RowVersion`. كما أن المشاريع الثلاثة تُبنى بنجاح، ونموذج EF Core متزامن حالياً مع آخر Migration.

مع ذلك، النسخة الحالية **غير جاهزة للنشر الإنتاجي**. توجد مشكلات حرجة في بيانات الحساب الافتراضي، وصلاحيات إدارة المستأجرين، والتسجيل العام، وسلامة العلاقات بين المستأجرين، إضافة إلى خطأ في إنهاء المعاملات قد يخفي الاستثناء الأصلي. كما توجد اختلافات بين DTOs والكيانات وقيود قاعدة البيانات يمكن أن تؤدي إلى فقدان دقة أو أخطاء وقت الحفظ.

**التقييم العام: 5.5/10**  
**الجاهزية للإنتاج: 4/10**

## التقييم التفصيلي

| الجانب | الدرجة | الملاحظة |
|---|---:|---|
| تصميم الكيانات | 7/10 | تنظيم جيد، لكن وراثة `Tenant` وبعض أنواع البيانات تحتاج تصحيحاً. |
| إعداد EF Core والعلاقات | 7/10 | إعدادات واضحة وفهارس جيدة، لكن سلامة العلاقات متعددة المستأجرين غير مفروضة في قاعدة البيانات. |
| Repository وUnit of Work | 6/10 | واجهات واضحة ودعم جيد للاستعلامات، مع خطأ فعلي في مسار فشل المعاملة وبعض المخاطر التصميمية. |
| DTOs والتحقق | 5/10 | التحقق غير متسق وتوجد اختلافات مع قيود قاعدة البيانات وأنواع الكيانات. |
| الأمان وعزل المستأجرين | 3/10 | توجد مسارات تسمح بإنشاء مستخدم وربطه بمستأجر، وإدارة المستأجرين دون صلاحيات إدارية مخصصة. |
| الاختبارات | 3/10 | لم يتم العثور على اختبارات موجهة للنماذج أو Repository أو العزل متعدد المستأجرين. |
| قابلية الصيانة | 6.5/10 | الفصل جيد، لكن التكرار والتسميات وبعض الاعتماديات تزيد تكلفة التطوير مستقبلاً. |

## نقاط القوة

- استخدام `BaseEntity` لتوحيد المعرفات وحقول التدقيق والحذف المنطقي و`RowVersion`.
- تطبيق Global Query Filters للحذف المنطقي ولتصفية كيانات المستأجر الحالي.
- فصل إعدادات العلاقات والفهارس وأطوال الحقول داخل ملفات `IEntityTypeConfiguration<T>`.
- استخدام `AsNoTracking` في معظم استعلامات القراءة.
- تمرير `CancellationToken` عبر عمليات الوصول إلى البيانات.
- وجود Specification Pattern للاستعلامات المركبة والتضمينات والترتيب.
- استخدام `decimal` للقيم المالية مع تحديد precision في أغلب الجداول.
- دعم المعاملات الصريحة للعمليات المركبة.
- استخدام `RowVersion` للمنافسة المتفائلة في الكيانات.
- نجاح بناء مشاريع Models وDTOs وDataAccess دون أخطاء أو تحذيرات.
- عدم وجود تغييرات معلقة بين نموذج EF Core وآخر Migration وقت إجراء المراجعة.

## الملاحظات الحرجة

### 1. حساب إداري افتراضي ببيانات دخول ثابتة

**الخطورة:** حرجة  
**الموقع:** `RetalSystemAPI.DataAccess/Seeders/DbInitializer.cs:67-85`، و`RetalSystemAPI/Program.cs:51-52`

ينشئ النظام المستخدم `masoud` بكلمة المرور الثابتة `Admin@123` عند تشغيل قاعدة بيانات جديدة، كما يتم استدعاء البذر في جميع البيئات دون تقييد واضح ببيئة التطوير.

**الأثر:** أي نشر جديد يحتفظ بهذه البيانات يمكن الدخول إليه بحساب معروف مسبقاً.

**الإصلاح المقترح:**

- منع إنشاء المستخدم الافتراضي خارج بيئة التطوير.
- قراءة بيانات الحساب الأول من Secret Manager أو متغيرات البيئة.
- توليد كلمة مرور عشوائية لمرة واحدة وإجبار المستخدم على تغييرها.
- فشل تشغيل التطبيق بوضوح إذا طُلب البذر الإنتاجي ولم تُمرر أسرار صالحة.

### 2. التسجيل العام يسمح باختيار المستأجر والفرع

**الخطورة:** حرجة  
**الموقع:** `RetalSystemAPI/Controllers/Auth/AuthController.cs:38-44`، و`RetalSystemAPI.Services/Auth/Implementations/AuthService.cs:42-73`

نقطة التسجيل متاحة عبر `[AllowAnonymous]`، ويستطيع العميل إرسال `TenantId` و`BranchId`. تتحقق الخدمة من وجود المستأجر فقط، ولا تتحقق من أن الفرع تابع للمستأجر المحدد.

**الأثر:** عند معرفة المعرفات يمكن إنشاء حساب مرتبط بمستأجر أو فرع غير مصرح به، كما يمكن إنشاء سجل مستخدم غير متسق تكون فيه هوية المستأجر مختلفة عن مستأجر الفرع.

**الإصلاح المقترح:**

- إغلاق التسجيل العام أو تحويله إلى نظام دعوات قصيرة العمر.
- استخراج `TenantId` من سياق موثوق بدلاً من جسم الطلب.
- التحقق من أن `Branch.TenantId == TenantId` قبل إنشاء المستخدم.
- إضافة قيد مركب في قاعدة البيانات يمنع ربط المستخدم بفرع تابع لمستأجر آخر.

### 3. عمليات إدارة المستأجرين لا تتطلب دوراً إدارياً

**الخطورة:** حرجة  
**الموقع:** `RetalSystemAPI/Controllers/Tenants/TenantsController.cs:102-174`

تعتمد عمليات عرض جميع المستأجرين وإنشائهم وتعديلهم وحذفهم على `[Authorize]` فقط، دون Policy أو Role خاص بمسؤول النظام.

**الأثر:** أي حساب مصادق يمكنه الوصول إلى عمليات إدارية على مستوى النظام، وليس مستوى مستأجره فقط.

**الإصلاح المقترح:**

- إنشاء Policy مثل `SystemAdminOnly`.
- حماية جميع عمليات الإدارة العامة بهذه السياسة.
- إبقاء مساري `GET /me` و`PUT /me` فقط للمستخدم العادي مع فرض معرف المستأجر من الـClaims.

### 4. خطأ في مسار فشل Commit

**الخطورة:** عالية  
**الموقع:** `RetalSystemAPI.DataAccess/Repositories/Implementations/UnitOfWork.cs:195-209`

عند فشل `SaveChangesAsync` أو `CommitAsync` تستدعي كتلة `catch` الدالة `RollbackTransactionAsync`. تقوم تلك الدالة بالتخلص من المعاملة وتعيين `_transaction = null`. بعد ذلك تحاول كتلة `finally` تنفيذ `_transaction.DisposeAsync()` مرة أخرى.

**الأثر:** ظهور `NullReferenceException` يخفي خطأ قاعدة البيانات الأصلي، ويصعّب التشخيص، وقد يؤدي إلى استجابة خطأ مضللة.

**الإصلاح المقترح:** الاحتفاظ بمرجع محلي للمعاملة والتخلص منه مرة واحدة فقط، أو جعل `RollbackTransactionAsync` لا تتنافس مع `finally` على ملكية التخلص من المعاملة.

## ملاحظات عالية ومتوسطة الخطورة

### 5. وراثة Tenant من TenantBaseEntity غير صحيحة دلالياً

**الخطورة:** عالية  
**الموقع:** `RetalSystemAPI.Models/Tenant.cs:14`

يرث `Tenant` من `TenantBaseEntity`، ولذلك يملك `TenantId` ومرجعاً إلى `Tenant` آخر. يحاول `TenantConfiguration` معالجة ذلك عبر تجاهل خاصية التنقل، بينما يبقى عمود `TenantId` في جدول `Tenants`.

**الأثر:** بيانات بلا معنى، وحالات يكون فيها `TenantId` فارغاً أو يشير إلى مستأجر منشئ السجل، وتعقيد غير ضروري في الفلاتر والتدقيق والهجرات.

**الإصلاح المقترح:** جعل `Tenant` يرث مباشرة من `BaseEntity`، ثم إنشاء Migration مدروسة لإزالة عمود `TenantId` من جدول `Tenants`.

### 6. قاعدة البيانات لا تفرض تطابق المستأجر في العلاقات

**الخطورة:** عالية  
**أمثلة:** `ApplicationUserConfiguration.cs:14-22`، و`WarehouseConfiguration.cs:31-39`، و`ProductConfiguration.cs:37-45`

يحتوي كل كيان على `TenantId`، لكن معظم العلاقات تستخدم معرف الكيان فقط، مثل `BranchId` أو `ProductId`. لا يوجد مفتاح خارجي مركب يضمن أن الكيان الأب والابن تابعان إلى المستأجر نفسه.

**الأثر:** خطأ في خدمة أو استيراد بيانات أو عملية إدارية قد ينشئ علاقة عابرة للمستأجرين لا تمنعها قاعدة البيانات.

**الإصلاح المقترح:** إضافة مفاتيح بديلة وفهارس ومفاتيح خارجية مركبة من الشكل `(TenantId, Id)` للعلاقات الحساسة، مع تحقق على مستوى الخدمة أيضاً.

### 7. AuditInterceptor يثق في TenantId الممرر

**الخطورة:** عالية  
**الموقع:** `RetalSystemAPI.DataAccess/Interceptors/AuditInterceptor.cs:55-65`

يتم تعيين المستأجر الحالي فقط إذا كان `TenantId == Guid.Empty`. إذا وصل كيان جديد بمعرف مستأجر آخر فلن يرفضه المعترض ولن يستبدله.

**الإصلاح المقترح:** في العمليات العادية عيّن `TenantId` دائماً من السياق الموثوق، وارفض الاختلاف صراحة. اجعل تجاوز ذلك ممكناً فقط لخدمة إدارية محددة وواضحة.

### 8. فقدان الجزء الكسري من كمية المخزون

**الخطورة:** عالية  
**الموقع:** `SetStorgeStockDto.cs:13-14`، و`SetShowroomStockDto.cs:13-14`، و`StockService.cs:138,145,272,279`

تستخدم DTOs النوع `decimal`، بينما تستخدم كيانات `StorgeStock` و`ShowroomStock` النوع `int`. تقوم الخدمة بالتحويل الصريح إلى `int`.

**الأثر:** القيمة `1.9` تصبح `1` بصمت، مما يسبب فقدان مخزون دون رسالة تحقق.

**الإصلاح المقترح:** اختيار سياسة موحدة: إما جعل المخزون `decimal` إذا كانت الوحدات الكسرية مدعومة، أو تغيير DTO إلى `int` ورفض الكسور.

### 9. اختلاف أطوال DTOs عن أعمدة قاعدة البيانات

**الخطورة:** عالية  
**أمثلة:**

- اسم المنتج: DTO يسمح بـ300، والعمود يسمح بـ200.
- اسم التصنيف: DTO يسمح بـ200، والعمود يسمح بـ150.
- اسم الفرع: DTO يسمح بـ200، والعمود يسمح بـ150.

**الأثر:** الطلب يجتاز Model Validation ثم يفشل وقت الحفظ بخطأ truncation أو DbUpdateException.

**الإصلاح المقترح:** تعريف ثوابت مشتركة لأطوال الحقول واستخدامها في DTO Validation وFluent API.

### 10. تحقق ناقص في DTOs الحساسة

**الخطورة:** متوسطة إلى عالية  
**الموقع:** `RetalSystemAPI.Models.DTOs/Purchase/PurchaseInvoiceDtos.cs:82-135`، و`RetalSystemAPI.Models.DTOs/Users/UserDtos.cs:23-45`

نماذج إنشاء وتحديث فواتير الشراء والمستخدمين تفتقد تحققاً أساسياً للأسماء والمعرفات وكلمات المرور والقيم المالية والقوائم.

**أمثلة:**

- قبول معرفات فارغة.
- قبول كمية أو سعر أو إجمالي سالب.
- قبول فاتورة بلا عناصر.
- قبول كلمة مرور فارغة في بعض DTOs الإدارية.
- قبول نص يتجاوز طول عمود قاعدة البيانات.

**الإصلاح المقترح:** إضافة Data Annotations متسقة أو اعتماد FluentValidation، مع اختبارات حدود لكل DTO.

### 11. Required لا يرفض Guid.Empty أو قيمة enum غير صالحة

**الخطورة:** متوسطة  
**أمثلة:** `CreateWarehouseDto.cs:12-16`، و`CreateProductDto.cs:21-22`

استخدام `[Required]` على `Guid` غير nullable لا يمنع `Guid.Empty`. كما أن استقبال enum لا يضمن أن الرقم المرسل عضو معرف في النوع.

**الإصلاح المقترح:** استخدام تحقق مخصص لـGuid، و`EnumDataType` أو FluentValidation للتحقق من `IsInEnum()`.

### 12. منطق التدقيق مكرر وغير متسق

**الخطورة:** متوسطة  
**الموقع:** `AppDbContext.cs:191-225`، و`AuditInterceptor.cs:40-88`

ينفذ `AppDbContext` جزءاً من التدقيق، وينفذ المعترض جزءاً آخر. في الحفظ غير المتزامن يعمل المساران، أما الحفظ المتزامن فلا ينفذ المعترض نسخته المتزامنة لأنها غير موجودة.

**الأثر:** اختلاف قيم وسلوك التدقيق حسب طريقة الحفظ، وتكرار مسؤوليات يزيد احتمال الانحراف مستقبلاً.

**الإصلاح المقترح:** وضع المنطق كله في معترض واحد يدعم `SavingChanges` و`SavingChangesAsync`، أو كله داخل `DbContext`، وليس كليهما.

### 13. سياسة كلمات المرور ضعيفة

**الخطورة:** متوسطة  
**الموقع:** `RetalSystemAPI.DataAccess/Extensions/DataAccessServiceExtensions.cs:57-65`

تسمح السياسة بكلمة مرور من ستة أحرف دون أرقام أو حالة أحرف أو رموز.

**الإصلاح المقترح:** رفع الحد الأدنى، وتفعيل متطلبات مناسبة، وإضافة lockout، ومنع كلمات المرور الشائعة، مع مراعاة أن الطول أهم من التعقيد الشكلي.

### 14. الفهارس الفريدة تتعارض مع الحذف المنطقي

**الخطورة:** متوسطة  
**أمثلة:** `ProductBarCodeConfiguration.cs:42-43`، و`SupplierPhoneConfiguration.cs:38`، و`UnitConfiguration.cs:37-38`

يبقى السجل المحذوف منطقياً مشاركاً في الفهرس الفريد، ولذلك لا يمكن إعادة استخدام باركود أو هاتف أو اسم وحدة بعد الحذف.

**الإصلاح المقترح:** استخدام filtered unique index مثل `[IsDeleted] = 0` للبيانات القابلة لإعادة الاستخدام، أو اعتماد الاستعادة بدلاً من إنشاء سجل جديد. يجب اتخاذ القرار حسب متطلبات أرقام الفواتير، لأنها قد تحتاج إلى البقاء فريدة تاريخياً حتى بعد الإلغاء.

### 15. التصفح يمكن أن يكون غير حتمي

**الخطورة:** متوسطة  
**الموقع:** `Repository.cs:145-170`

يسمح `GetPagedAsync` بتنفيذ `Skip/Take` دون ترتيب إذا كانت `orderBy` فارغة. لا تضمن قاعدة البيانات ترتيباً ثابتاً في هذه الحالة.

**الإصلاح المقترح:** فرض ترتيب افتراضي ثابت مثل `CreatedAt` ثم `Id`، والتحقق من أن `pageNumber >= 1` وأن `pageSize` ضمن حد أعلى معقول.

### 16. واجهة Repository تخالف تنفيذ GetByIdAsync

**الخطورة:** منخفضة إلى متوسطة  
**الموقع:** `IRepository.cs:19-25`، و`Repository.cs:40-43`

توثق الواجهة أن `GetByIdAsync` يعيد كياناً بلا تتبع، لكن التنفيذ يستخدم `FindAsync` الذي يعيد كياناً متتبعاً وقد يعيده أولاً من ChangeTracker.

**الإصلاح المقترح:** توضيح العقد وتسميته كاستعلام متتبع، أو استخدام استعلام `AsNoTracking().SingleOrDefaultAsync` وإضافة دالة منفصلة للنسخة المتتبعة.

## ملاحظات معمارية وقابلية الصيانة

### اعتماد مشروع Models على EF Core SQL Server وأدوات التصميم

يحتوي `RetalSystemAPI.Models.csproj` على حزم EF Core وSQL Server وDesign وTools. هذا يجعل طبقة النماذج مرتبطة بتقنية التخزين أكثر من اللازم.

**الاقتراح:** إبقاء الاعتمادية الدنيا المطلوبة لتعريف المستخدم، ونقل الحزم الخاصة بـEF Core وSQL Server والهجرات إلى DataAccess فقط.

### تضخم UnitOfWork

تحتوي `IUnitOfWork` و`UnitOfWork` على خاصية وحقل لكل نوع كيان، رغم وجود Generic Repository مسجل في DI.

**الاقتراح:** إما حقن `IRepository<T>` مباشرة، أو إضافة `Repository<T>()` عامة داخل وحدة العمل، مع إبقاء مستودعات مخصصة فقط للاستعلامات التي تحتاج سلوكاً خاصاً.

### تسميات غير متسقة

أمثلة: `StorgeStock` بدلاً من `StorageStock`، و`Branchs` بدلاً من `Branches`، و`BarcodeId`/`ProductBarcodeId`/`BarcodeCode` لتمثيل المفهوم نفسه.

**الأثر:** زيادة أخطاء الكتابة، وتسريب أسماء غير صحيحة إلى الجداول والعقود العامة، وصعوبة التصحيح بعد استقرار API.

**الاقتراح:** وضع خطة Rename مع Migration واضحة قبل اعتماد النسخة الإنتاجية.

### كتم تحذير تغير نموذج EF

يقوم `DataAccessServiceExtensions` بكتم `PendingModelChangesWarning`. النموذج متزامن حالياً، لكن كتم التحذير قد يخفي مشكلة مستقبلية.

**الاقتراح:** إزالة الكتم وإضافة فحص `has-pending-model-changes` إلى CI.

## الاختبارات المطلوبة

لم يتم العثور على اختبارات موجهة مباشرة لطبقة Models أو DataAccess. يوصى بإضافة اختبارات تكامل باستخدام SQL Server حقيقي أو حاوية اختبار، لأن SQLite لا يحاكي جميع خصائص SQL Server مثل `rowversion` والفهارس المرشحة بدقة.

الحد الأدنى المقترح:

1. مستخدم من المستأجر A لا يستطيع قراءة أو تعديل أو حذف بيانات المستأجر B.
2. لا يمكن إنشاء علاقة بين كيانين من مستأجرين مختلفين.
3. `Guid.Empty` والقيم غير المعرفة للـenum تُرفض.
4. فشل Commit يعيد الاستثناء الأصلي ولا ينتج `NullReferenceException`.
5. تعارض `RowVersion` ينتج نتيجة Concurrency واضحة.
6. الحذف المنطقي يخفي السجل من جميع الاستعلامات العادية.
7. سياسة إعادة استخدام القيم الفريدة بعد الحذف تعمل كما هو مطلوب.
8. إدخال كمية كسرية لا يؤدي إلى قطعها بصمت.
9. حدود أطوال DTOs مطابقة لأعمدة قاعدة البيانات.
10. الحساب العادي لا يستطيع استدعاء عمليات إدارة المستأجرين.

## خطة الإصلاح المقترحة

### المرحلة الأولى: قبل أي نشر

1. إزالة بيانات الحساب الافتراضي الثابتة.
2. إغلاق التسجيل العام أو تحويله إلى دعوات.
3. حماية إدارة المستأجرين بسياسة `SystemAdminOnly`.
4. التحقق من تطابق الفرع والمستأجر عند إنشاء المستخدم.
5. إصلاح مسار Commit/Rollback في `UnitOfWork`.

### المرحلة الثانية: سلامة البيانات

1. تغيير وراثة `Tenant` إلى `BaseEntity` مع Migration.
2. فرض المستأجر الحالي عند إضافة الكيانات.
3. إضافة العلاقات المركبة الحساسة متعددة المستأجرين.
4. توحيد نوع كمية المخزون.
5. توحيد حدود DTOs وFluent API.
6. إضافة تحقق شامل لفواتير الشراء والمستخدمين.

### المرحلة الثالثة: الاعتمادية والصيانة

1. توحيد منطق التدقيق.
2. مراجعة الفهارس الفريدة مع الحذف المنطقي.
3. فرض ترتيب وحدود آمنة للتصفح.
4. إزالة حزم EF غير المطلوبة من Models.
5. توحيد التسميات.
6. إضافة اختبارات التكامل وفحص Migration إلى CI.

## نتائج التحقق المنفذة

- `dotnet build RetalSystemAPI.Models/RetalSystemAPI.Models.csproj --no-restore`: نجح، 0 أخطاء، 0 تحذيرات.
- `dotnet build RetalSystemAPI.Models.DTOs/RetalSystemAPI.Models.DTOs.csproj --no-restore`: نجح، 0 أخطاء، 0 تحذيرات.
- `dotnet build RetalSystemAPI.DataAccess/RetalSystemAPI.DataAccess.csproj --no-restore`: نجح، 0 أخطاء، 0 تحذيرات.
- `dotnet ef migrations has-pending-model-changes`: لا توجد تغييرات معلقة وقت المراجعة.

## الخلاصة

البنية الحالية مناسبة كأساس لنظام إدارة مبيعات ومخزون متعدد المستأجرين، لكنها تحتاج إلى تشديد واضح لحدود الأمان وسلامة البيانات قبل الإنتاج. بعد إغلاق مشكلات الحساب الافتراضي والتسجيل والصلاحيات والعلاقات العابرة للمستأجرين، ثم توحيد DTOs وإضافة الاختبارات، يمكن أن يرتفع التقييم إلى نطاق **8/10** مع جاهزية إنتاجية جيدة.
