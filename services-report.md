# تقرير تحليل وتقييم طبقة الخدمات

**المشروع:** RetalSystemAPI  
**تاريخ التحليل:** 2026-09-11  
**النطاق:** `RetalSystemAPI.Services` مع مراجعة حدودها المباشرة في Controllers وDTOs وDataAccess عند الحاجة للتحقق من السلوك.  
**ملاحظة:** التحليل مبني على الحالة الحالية لمجلد العمل، والذي يحتوي على تعديلات محلية كثيرة غير مثبتة في Git.

## الملخص التنفيذي

طبقة الخدمات منظمة من ناحية تقسيم المجالات واستخدام الواجهات و`ServiceResult` و`CancellationToken`، وتنجح في البناء دون أخطاء أو تحذيرات. لكن توجد عيوب حرجة في التفويض، وتحديث المخزون، وحسابات الفواتير والمرتجعات؛ لذلك لا أوصي بنشر النسخة الحالية في بيئة إنتاج قبل معالجة البنود ذات الأولوية P0 وP1.

**التقييم العام: 4.0 / 10 — غير جاهزة للإنتاج حالياً.**

| المحور | التقييم | الملاحظة المختصرة |
|---|---:|---|
| التنظيم وفصل المسؤوليات | 7/10 | تقسيم جيد إلى Interfaces/Implementations/Specifications |
| صحة منطق الأعمال | 3/10 | أخطاء مؤثرة في الفواتير والمخزون والمرتجعات |
| الأمن والتفويض | 2/10 | المصادقة موجودة، لكن التفويض حسب الدور شبه غائب |
| اتساق البيانات والمعاملات | 3/10 | عمليات مالية ومخزنية قابلة للتجزؤ أو عدم التطابق |
| التحقق من المدخلات | 4/10 | غير متسق، وبعض DTOs المالية بلا DataAnnotations |
| الأداء وقابلية التوسع | 4/10 | N+1 وتحميل كامل للجداول وعمليات Excel غير محدودة |
| القابلية للاختبار والصيانة | 5/10 | الواجهات مفيدة، لكن لا توجد اختبارات خدمات آلية ظاهرة |

## نطاق الفحص والتحقق

- 66 ملف C# في مشروع الخدمات: 24 ملف Implementations، و24 Interfaces، و14 Specifications، إضافة إلى النماذج المشتركة والتسجيل.
- مراجعة خدمات المصادقة والمستخدمين والكتالوج والمبيعات والمشتريات والمخزون والمستأجرين والملفات ولوحة التحكم.
- مراجعة Controllers للتأكد من سياسات الوصول، وDTOs للتأكد من حدود التحقق.
- تنفيذ:

```text
dotnet build RetalSystemAPI.Services/RetalSystemAPI.Services.csproj --no-restore
```

النتيجة: **نجح البناء، 0 أخطاء، 0 تحذيرات**.

لم يُعثر على مشروع اختبارات وحدات أو تكامل مخصص لطبقة الخدمات؛ المشروع الموجود تحت `tests` هو `RetalSystemAPI.Desktop.UxChecks` فقط.

## النتائج الحرجة (P0)

### 1. أي مستخدم مسجل يستطيع تنفيذ وظائف إدارية شديدة الحساسية

جميع المتحكمات تقريباً تستخدم `[Authorize]` فقط، ولا توجد سياسات `Role` أو `Policy` مخصصة. خدمات المستخدمين والمستأجرين والمخزون لا تطبق تفويضاً داخلياً كذلك. بناء عليه يستطيع أي مستخدم مصادق عليه — بحسب المسارات الحالية — إنشاء مستخدمين، تغيير الأدوار، إعادة تعيين كلمات مرور مستخدمين آخرين، تعديل المخزون، وإدارة المستأجرين.

الأثر أخطر في `UserService`:

- يسمح `CreateUserAsync` باختيار `TenantId` من الطلب بدلاً من إجبار المستأجر الحالي: `UserService.cs:156-164`.
- ينشئ أي دور غير موجود ثم يسنده للمستخدم: `UserService.cs:176-180`.
- يسمح التحديث باستبدال كل أدوار المستخدم بدور قادم من الطلب: `UserService.cs:235-245`.
- نقطة إعادة تعيين كلمة المرور متاحة لأي مستخدم مصادق عليه في `UsersController.cs:89-93`.

**النتيجة المحتملة:** تصعيد صلاحيات، إنشاء حساب عبر مستأجر آخر، أو الاستيلاء على حساب داخل المستأجر.

**الإصلاح:** تعريف سياسات واضحة مثل `SystemAdmin`, `TenantAdmin`, `InventoryManager`, `Cashier`، وتطبيقها على المتحكمات وعلى حالات الاستخدام الحساسة داخل الخدمات. يجب اشتقاق `TenantId` من السياق الموثوق فقط، والتحقق من أن الفرع يتبع المستأجر، وحظر إنشاء الأدوار ديناميكياً من مدخل المستخدم.

### 2. تحديث فاتورة مبيعات يخصم المخزون مرتين

في `SalesInvoiceService.UpdateAsync` تتم محاولة إعادة الكميات القديمة إلى المخزون باستخدام `FirstOrDefaultAsync` في `SalesInvoiceService.cs:277-297`. هذا الاستعلام غير متتبع وفق مستودع البيانات الحالي، ثم تُعدّل الكمية دون استدعاء `Update`. لذلك لا تُحفظ الإعادة. بعد ذلك تُخصم الكميات الجديدة فعلياً وتُحدّث السجلات في `SalesInvoiceService.cs:352-377`.

**النتيجة المحتملة:** كل تعديل لبنود الفاتورة يخصم الكمية الجديدة فوق الخصم السابق، فتتآكل الأرصدة أو تصبح سالبة. كما أن فحص الكفاية لا يحتسب الإعادة القديمة فعلياً.

**الإصلاح:** تحميل سجلات المخزون tracked مرة واحدة، تجميع القديم والجديد حسب مفتاح المخزون، ثم تطبيق صافي الفرق داخل معاملة واحدة. يجب إضافة اختبار تكامل يثبت أن تعديل فاتورة من كمية 3 إلى 5 يغيّر المخزون بمقدار 2 فقط.

### 3. تحديث فاتورة مشتريات لا يزامن البنود الحقيقية مع الطلب

منطق `PurchaseInvoiceService.UpdateAsync` يقارن عدد البنود فقط:

- إذا تساوى العدد لا يتم تحديث أي بند مخزن، حتى لو تغير المنتج أو السعر أو الكمية.
- إذا زاد العدد تضاف العناصر الموجودة بعد موضع `existingCount` فقط: `PurchaseInvoiceService.cs:190-234`.
- إذا نقص العدد تحذف العناصر الأخيرة حسب ترتيب القائمة لا حسب هوية البند: `PurchaseInvoiceService.cs:235-256`.
- في الوقت نفسه يعاد حساب رأس الفاتورة من DTO الجديد: `PurchaseInvoiceService.cs:266-290`.

يمكن بالتالي أن تعرض الفاتورة إجماليات لا تطابق بنودها المسجلة، بينما يصبح المخزون غير متوافق مع الاثنين.

يوجد خلل إضافي عند تغيير المستودع: نقل المخزون بين المستودعين يحدث فقط عندما تكون `dto.Items == null` في `PurchaseInvoiceService.cs:259-264`. إذا أرسلت الواجهة قائمة البنود المعتادة، يتغير `WarehouseId` في الرأس دون نقل الرصيد القديم.

**الإصلاح:** إعطاء البنود معرفات في Update DTO أو تنفيذ استبدال كامل مضبوط؛ حساب diff حسب الهوية، وعكس/تطبيق أثر كل فرق والمستودع في معاملة واحدة. يمنع تحديث الرأس بمعزل عن البنود.

### 4. العميل يتحكم في القيم المالية الحرجة لفاتورة المبيعات

عند الإنشاء تعتمد الخدمة على `TotalAmount`, `PaidAmount` و`UnitCost` القادمة من العميل:

- `RemainingAmount = dto.TotalAmount - dto.PaidAmount`: `SalesInvoiceService.cs:188-190`.
- `UnitCost` ينسخ من DTO إلى بند الفاتورة: `SalesInvoiceService.cs:192-200`.

لا يعاد احتساب رأس الفاتورة من البنود عند الإنشاء، ولا تُقرأ تكلفة المنتج من مصدر موثوق. ويمكن أن يتجاوز الخصم قيمة السطر أو تتجاوز الدفعة الإجمالي، فتنتج قيم سالبة أو أرباح غير صحيحة. لوحة التحكم تعتمد لاحقاً على `UnitCost` لحساب الربح.

**النتيجة المحتملة:** تلاعب بالإيراد والديون والأرباح والتقارير، حتى لو كان خصم المخزون صحيحاً.

**الإصلاح:** تجاهل المجاميع والتكلفة القادمة من العميل؛ جلب السعر/التكلفة وفق السياسة المعتمدة، حساب كل سطر ثم SubTotal/Discount/Tax/Total/Remaining على الخادم، ورفض الخصم أو الدفع خارج الحدود.

### 5. المرتجعات لا ترتبط فعلياً بالفواتير الأصلية

`SalesReturnService` يتحقق فقط من وجود الفاتورة الأصلية (`SalesReturnService.cs:126-133`) ولا يتحقق من أن العميل/الفرع/المستودع متطابق، أو أن المنتج والباركود بيع فعلاً، أو أن مجموع المرتجع السابق والحالي لا يتجاوز الكمية المباعة. ثم يضيف الكمية مباشرة للمخزون (`SalesReturnService.cs:174-218`).

`PurchaseReturnService` يعاني المشكلة نفسها مع فاتورة الشراء (`PurchaseReturnService.cs:139-153`): لا يطابق البنود ولا يمنع تجاوز الكمية المشتراة. والأسوأ أن فحص الكمية يستخدم `Ceiling` عند السطر 188، بينما الخصم والاسترجاع يستخدمان `(int)` عند السطرين 231 و270؛ أي أن القيمة العشرية المسجلة لا تطابق حركة المخزون.

كما يجري فحص بنود مرتجع المشتريات واحداً واحداً دون تجميع الباركود المتكرر. قد ينجح بندان كل منهما 6 أمام رصيد 10، ثم يصبح المخزون -2 عند التنفيذ.

**الإصلاح:** التحقق من المرجع والبنود والكميات التراكمية داخل معاملة، وتجميع المطلوب حسب مفتاح المخزون. يجب توحيد وحدة الكمية: إما أعداد صحيحة في DTO/Entity أو decimal في كامل نموذج المخزون دون قص أو تقريب ضمني.

## نتائج عالية الخطورة (P1)

### 6. تعطيل المستخدم لا يمنع تسجيل دخوله

`UserService` يمثل التعطيل بوضع `LockoutEnd = DateTimeOffset.MaxValue` (`UserService.cs:217-225`)، لكن `AuthService.LoginAsync` يستخدم `CheckPasswordAsync` فقط (`AuthService.cs:96`) ولا يستدعي فحص القفل ولا يتحقق من نشاط المستأجر أو الفرع. يمكن للحساب المعطل الحصول على JWT جديد.

كذلك `LogoutAsync` لا يبطل شيئاً (`AuthService.cs:107-110`)، و`RefreshToken` يعاد فارغاً (`AuthService.cs:165`).

**الإصلاح:** استخدام `SignInManager.CheckPasswordSignInAsync` أو فحص `IsLockedOutAsync` صراحة، وفحص حالة المستخدم/المستأجر/الفرع. إما تنفيذ refresh-token rotation وrevocation فعلياً أو حذف هذا العقد من الاستجابة وتوضيح أن JWT قصير العمر غير قابل للإبطال.

### 7. مفتاح JWT الاحتياطي غير متطابق بين الإصدار والتحقق

`AuthService` يوقع بالمفتاح الاحتياطي `DefaultSuperSecretKey...` في `AuthService.cs:119`، بينما `Program.cs:20` يستخدم مفتاحاً احتياطياً مختلفاً للتحقق. عند غياب `JwtSettings:Secret` ستصدر رموز لا يقبلها التطبيق نفسه. وجود مفاتيح سرية ثابتة كقيمة fallback غير مناسب أمنياً أيضاً.

**الإصلاح:** عدم تشغيل التطبيق عند غياب السر، والتحقق منه عند startup، واستخدام Options موحدة ومتحقق منها في التوقيع والتحقق.

### 8. حالات الفواتير لا تملك آلة انتقال موحدة

- فاتورة المبيعات تخصم المخزون عند الإنشاء مهما كانت `Draft` أو `Pending`: `SalesInvoiceService.cs:203-234`.
- إلغاء فاتورة المبيعات يعيد المخزون، لكن يمكن تغيير الحالة لاحقاً من Cancelled/Voided إلى حالة نشطة دون خصمه من جديد لأن الحماية ليست آلة حالات مكتملة: `SalesInvoiceService.cs:426-477`.
- فاتورة المشتريات تزيد المخزون وتغيّر تكلفة المنتج فور الإنشاء مهما كانت الحالة: `PurchaseInvoiceService.cs:150-156`.
- إلغاء فاتورة مشتريات يعكس الكمية، لكنه لا يعكس تغيير `CostPrice/AveragePrice` السابق.

**الإصلاح:** تحديد حالة ترحيل واحدة تؤثر في المخزون، وإنشاء جدول انتقالات مسموحة، وجعل الإلغاء/العكس حركة محاسبية ومخزنية واضحة غير قابلة للتكرار.

### 9. متوسط تكلفة الشراء محسوب رياضياً بطريقة غير صحيحة

`PurchaseInvoiceService.cs:426-442` يحدّث `AveragePrice` عبر `(oldAverage + newPrice) / 2`. هذه ليست تكلفة متوسطة مرجحة لأنها تتجاهل كمية الرصيد الحالي وكمية الشراء.

**الإصلاح:** استخدام `(oldStockValue + receivedValue) / (oldQuantity + receivedQuantity)`، مع قرار واضح لكيفية المرتجعات والإلغاء وإعادة تقييم المخزون.

### 10. التسوية الجردية تسجل فرقاً يمكن للعميل تزويره

`StockAdjustmentService` يأخذ `SystemQuantity` من DTO ويحسب الفرق منه (`StockAdjustmentService.cs:119-126`) بدلاً من قراءة الرصيد الفعلي. بعدها يستبدل الرصيد بـ`ActualQuantity`. كما أن حذف التسوية يخفي السجل فقط ولا يعكس الحركة (`StockAdjustmentService.cs:188-199`).

**الإصلاح:** قراءة SystemQuantity من قاعدة البيانات في نفس المعاملة، وحفظ before/after والمستخدم والسبب. يفضل منع حذف التسويات المرحلة واستبداله بحركة عكسية مرتبطة بالسجل الأصلي.

### 11. إنشاء المنتج قد ينشئ نوع رصيد غير مناسب للمستودع وبعملية غير ذرية

إذا لم توجد صالات عرض، تستخدم الخدمة جميع المستودعات كبديل وتضيف لها `ShowroomStock` (`ProductService.cs:154-194`). وإذا لم توجد مخازن تخزين تفعل الشيء نفسه مع `StorgeStock` (`ProductService.cs:196-247`). هذا يربط نوع رصيد بمستودع من نوع مخالف.

كما تُحفظ بيانات المنتج أولاً في `ProductService.cs:151-152`، ثم تحفظ سجلات المخزون في عملية ثانية عند السطر 250. فشل المرحلة الثانية يترك منتجاً ناقص التهيئة. النمط نفسه موجود عند إضافة باركود في `ProductBarCodeService.cs:103-119`.

**الإصلاح:** حذف fallback إلى جميع المستودعات، والتحقق من معرفات المستودعات المرسلة، وحفظ المنتج والباركود والأرصدة في معاملة واحدة.

### 12. خدمة رفع وحذف الملفات معرضة لمسارات غير موثوقة

- التحقق يعتمد على امتداد اسم الملف فقط، لا على MIME أو signature/فك ترميز الصورة: `FileUploadService.cs:121-123`.
- `subfolder` يدخل مباشرة في `Path.Combine`: `FileUploadService.cs:59`.
- الحذف يحول `fileUrl` إلى مسار محلي دون canonicalization وفحص أنه داخل `wwwroot/uploads`: `FileUploadService.cs:101-106`.
- تعاد رسالة الاستثناء الداخلية للعميل: `FileUploadService.cs:80`.
- أخطاء الحذف تُبتلع ويعاد نجاح، مما يخفي فشل تنظيف الملف.

**النتيجة المحتملة:** كتابة/حذف خارج المجلد المقصود إذا وصل مسار متحكم فيه، رفع محتوى متنكر، وتسريب تفاصيل الخادم.

**الإصلاح:** جعل المجلد قيمة enum/ثابتة داخلية، استخدام `Path.GetFullPath` ثم فحص prefix تحت جذر uploads، توليد الاسم داخلياً، فحص الحجم والتوقيع وفك الصورة، وعدم إرجاع `ex.Message`.

### 13. استيراد Excel غير محدود ويمكن أن يستنزف الذاكرة

المتحكم يتحقق فقط من أن الملف غير فارغ (`ProductsController.cs:143-167`). الخدمة تنسخ كامل stream إلى الذاكرة ثم تبني `XLWorkbook` (`ProductExcelService.cs:247-259` و`607-619`). لا يوجد حد للحجم أو عدد الصفوف/الخلايا أو timeout خاص، ما يعرض النظام لملفات ضخمة أو zip bombs.

كذلك يحمل التصدير والاستيراد جداول المنتجات والباركودات والتصنيفات والوحدات وكامل المخزون إلى الذاكرة (`ProductExcelService.cs:42-48`, `264-266`, `726-742`) ويجري عمليات بحث خطية متكررة داخل الحلقات. وتُعاد `ex.Message` للعميل في مساري التحقق والاستيراد (`ProductExcelService.cs:592-595`, `658-662`).

**الإصلاح:** حد request واضح، فحص امتداد ومحتوى XLSX، حد صفوف وأعمدة، معالجة batch/stream قدر الإمكان، تحويل القوائم إلى Dictionaries/HashSets، وتسجيل الاستثناء داخلياً مع رسالة عامة.

## نتائج متوسطة (P2)

### 14. القراءة من المخزون تغيّر قاعدة البيانات

`GetStorgeStocksByWarehouseAsync` و`GetShowroomStocksByWarehouseAsync` تنشئ سجلات رصيد صفرية لكل باركود/منتج مفقود أثناء GET (`StockService.cs:52-83` و`186-217`). هذا يجعل القراءة ذات أثر جانبي، وقد ينشئ عدداً ضخماً من السجلات ويزيد احتمالات التعارض.

**الإصلاح:** عرض الصفوف المفقودة كإسقاط بقيمة صفرية دون حفظ، أو تنفيذ التهيئة في event/job مستقل عند إنشاء المنتج/المستودع.

### 15. قص صامت للكميات العشرية

DTOs الخاصة بضبط المخزون تستخدم `decimal` بينما الكيان يستخدم `int`، وتنفذ الخدمة `(int)dto.Quantity` في `StockService.cs:133-146` و`267-280`. خدمات المشتريات تستخدم القص نفسه في عدة مواضع. يؤدي هذا إلى فقد كسور دون رسالة خطأ.

**الإصلاح:** توحيد نوع الكمية عبر النظام، أو رفض أي قيمة غير صحيحة قبل التحويل وعدم استخدام cast صامت.

### 16. التحقق من العلاقات المركبة ناقص

تتحقق عدة خدمات من وجود الفرع والمستودع والباركود كل على حدة، لكنها لا تتحقق من العلاقات التالية:

- أن المستودع يتبع الفرع في فواتير البيع والشراء وأوامر الشراء.
- أن الباركود يتبع `ProductId` في الفواتير والتحويلات والصور.
- أن فرع المستخدم يتبع Tenant المستخدم.
- أن كل breakdown يتبع المنتج وأن مجموع breakdowns يساوي كمية البند.

**الإصلاح:** Validators مركزية لحدود aggregate، واستخدام مفاتيح/قيود مركبة في قاعدة البيانات كخط دفاع ثانٍ.

### 17. التحويلات المخزنية تحتاج تحققاً أعمق

الخدمة تجمع الكميات قبل الخصم، وهذه نقطة جيدة، لكنها لا تتحقق أثناء الإنشاء من وجود كل منتج/باركود أو تطابقهما، ولا تفرض وجود باركود عندما يكون المصدر/الوجهة مخزن تخزين (`StockTransferService.cs:99-139`). العنصر بلا باركود قد يمر إلى Completed دون حركة كاملة في أحد الطرفين.

**الإصلاح:** بناء بنود normalized بعد معرفة نوع المصدر والوجهة، ورفض أي بند لا يمكن تمثيله على الجهتين.

### 18. أداء غير مستقر مع نمو البيانات

- `UserService.GetPagedUsersAsync` يجلب أدوار كل مستخدم باستعلام مستقل (`UserService.cs:81-84`)؛ نمط N+1.
- إنشاء مستودع يحمل كل المنتجات أو الباركودات ويولّد سجلاً لكل واحد (`WarehouseService.cs:88-123`).
- عدة `GetAllAsync` غير مقيدة في الخدمات، مع عدم توحيد التحقق من `pageNumber/pageSize`.
- Dashboard ينفذ عدداً كبيراً من الاستعلامات المتسلسلة ويحمل فواتير وفروعها إلى الذاكرة لحساب الاتجاه (`DashboardService.cs:55-139`, `204-247`). كما أن `days` و`count` غير محدودين.

**الإصلاح:** projections واستعلامات مجمعة في SQL، pagination افتراضية وحد أقصى مركزي، batch inserts، وقيود للمعاملات العددية.

### 19. مشاكل وقت وتقارير

Dashboard يستخدم `DateTime.Now` لتحديد حدود اليوم (`DashboardService.cs:34-38`, `206-207`) بينما بقية النظام تسجل غالباً `UtcNow`. إذا كانت قاعدة البيانات تحفظ UTC فستكون تقارير اليوم خاطئة قرب حدود اليوم. كذلك `PercentageOfTotalSales` يحسب نسبة كل منتج إلى مجموع أفضل المنتجات فقط، لا إلى إجمالي المبيعات (`DashboardService.cs:260-290`).

**الإصلاح:** اعتماد `TimeProvider` ومنطقة زمنية موحدة وتحويل الحدود إلى UTC قبل الاستعلام، وتصحيح مقام النسبة أو تغيير اسم الحقل.

### 20. عدم ذرية عمليات الملفات مع قاعدة البيانات

في حذف صورة منتج يحذف الملف أولاً ثم يحذف سجل قاعدة البيانات (`ProductImageService.cs:81-93`). إذا فشل الحفظ يبقى السجل مشيراً إلى ملف غير موجود. والعكس محتمل عند الرفع إذا نجح حفظ الملف وفشل حفظ السجل.

**الإصلاح:** نمط compensating action: عند فشل DB بعد الرفع يحذف الملف الجديد، وعند الحذف تنقل الصورة مؤقتاً أو تحذف السجل أولاً ثم ينفذ cleanup موثوقاً عبر outbox/job.

### 21. أخطاء اتساق وصيانة

- تستخدم خدمة فاتورة المشتريات رموز خطأ تخص أمر الشراء مثل `PurchaseOrderNotFound` و`PurchaseOrderNumberExists`: `PurchaseInvoiceService.cs:65,96,170`.
- `ServicesLayerExtensions` يستقبل `IConfiguration` ولا يستخدمه.
- يسجل AutoMapper نفس assembly عدة مرات لأن ملفات profiles المذكورة كلها في مشروع DTO نفسه: `ServicesLayerExtensions.cs:45-52`.
- التعامل مع نتائج Identity غير متسق؛ نتائج إنشاء الدور وإزالة/إضافة الأدوار لا يتم التحقق منها، وقد تنجح بعض خطوات تحديث المستخدم وتفشل بقية الخطوات دون rollback.
- حالات `DbUpdateConcurrencyException` تعالج في مواضع محدودة فقط، رغم أن كيانات كثيرة تستخدم RowVersion.

## نقاط جيدة في التصميم الحالي

- فصل المجالات واضح، مع واجهة لكل خدمة تقريباً.
- نمط `ServiceResult<T>` يوحد النجاح والفشل ورموز الأخطاء.
- تمرير `CancellationToken` موجود في معظم المسارات.
- استخدام Specifications يقلل تكرار تفاصيل الاستعلامات ويساعد في عزل DataAccess.
- معظم العمليات التي تحفظ aggregate ومخزونه تستعمل `SaveChanges` واحدة، ما يوفر ذرية على مستوى قاعدة البيانات عندما لا يوجد حفظ وسيط.
- تجميع الكميات قبل فحص مخزون فاتورة المبيعات والتحويل المخزني فكرة صحيحة وتمنع تجاوز الرصيد بسبب تكرار نفس المنتج في الطلب.
- استعمال `AsNoTracking` في مسارات القراءة مفيد، بشرط عدم محاولة تعديل الكيانات الناتجة كما حدث في تحديث فاتورة المبيعات.

## خطة إصلاح مقترحة

### المرحلة 1 — إيقاف مخاطر الإنتاج

1. إضافة سياسات الأدوار وإغلاق إدارة المستخدمين والمستأجرين والمخزون أمام المستخدم العادي.
2. إصلاح تحديث فاتورة المبيعات ليطبق صافي فرق المخزون.
3. إعادة كتابة مزامنة بنود فاتورة المشتريات وتغيير المستودع.
4. جعل كل القيم المالية والكلفة محسوبة على الخادم.
5. ربط المرتجعات بالفاتورة الأصلية ومنع تجاوز الكمية.
6. منع دخول المستخدم/المستأجر/الفرع المعطل وتوحيد إعداد JWT.

### المرحلة 2 — توحيد قواعد المخزون

1. تعريف خدمة مركزية `IInventoryMovementService` تسجل حركات immutable بدلاً من تعديل الرصيد من عدة خدمات.
2. تعريف واضح للحالات التي ترحّل المخزون وآلة انتقال حالات.
3. توحيد نوع الكمية وقواعد التقريب.
4. تطبيق قيود العلاقات المركبة والتحقق من Warehouse↔Branch وBarcode↔Product.
5. حماية التزامن وإرجاع خطأ 409 واضح عند تعارض RowVersion.

### المرحلة 3 — الصلابة والأداء

1. تأمين الملفات وExcel ووضع حدود للحجم والصفوف.
2. إزالة N+1 وتحويل العمليات الشاملة إلى batch/projection.
3. منع GET من الكتابة في قاعدة البيانات.
4. توحيد pagination والتحقق من الحدود.
5. إضافة logging منظم دون كشف رسائل الاستثناء للعميل.

### المرحلة 4 — الاختبارات المطلوبة قبل النشر

- اختبارات تفويض لكل دور ولكل endpoint حساس.
- اختبار عزل المستأجر عند الإنشاء والتحديث والبحث.
- اختبارات تكامل لحركات المخزون: بيع، تعديل، إلغاء، مرتجع، شراء، نقل، وتسوية.
- اختبارات invariant: لا مخزون سالب، مجموع الرأس يساوي البنود، والمرتجع لا يتجاوز الأصل.
- اختبارات انتقال الحالات وعدم تكرار الترحيل أو العكس.
- اختبارات تزامن لعمليتي بيع متوازيتين على آخر كمية.
- اختبارات ملفات ضارة/كبيرة ومسارات traversal وملفات XLSX تالفة.

## معايير قبول مختصرة للنسخة الآمنة

- لا يستطيع المستخدم العادي إنشاء مستخدم Admin أو إعادة تعيين كلمة مرور غيره.
- لا يمكن لأي طلب اختيار TenantId خارج سياق JWT الموثوق.
- تعديل فاتورة لا يغير المخزون إلا بصافي فرق البنود.
- كل فاتورة تحقق `Total = Sum(lines) - discounts + tax` على الخادم.
- لا يقبل مرتجعاً لمنتج غير موجود في الأصل أو بكمية تراكمية أكبر من الأصل.
- لا تؤثر Draft/Pending في المخزون إلا إذا كانت هذه قاعدة أعمال موثقة ومختبرة صراحة.
- لا ينتج أي مسار مخزوناً سالباً، بما في ذلك الطلبات ذات البنود المكررة والمتزامنة.
- لا يستطيع مسار الملفات الخروج من جذر uploads، ولا يقبل ملفاً اعتماداً على الامتداد وحده.
- توجد اختبارات خدمات وتكامل آلية تمر بالكامل إلى جانب نجاح البناء.

## الخلاصة

البنية العامة قابلة للتحسين وليست بحاجة إلى إعادة بناء المشروع من الصفر، لكن منطق المخزون والفواتير يحتاج إعادة ضبط مركزية قبل إضافة ميزات جديدة. أعلى عائد سيكون من: إغلاق الصلاحيات أولاً، ثم توحيد حركة المخزون والحالات، ثم جعل الحسابات المالية مشتقة من بيانات موثوقة، وأخيراً تغطية هذه القواعد باختبارات تكامل تمنع رجوع الأخطاء.
