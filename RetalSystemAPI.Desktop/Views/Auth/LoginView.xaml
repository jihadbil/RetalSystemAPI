<UserControl x:Class="RetalSystemAPI.Desktop.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:RetalSystemAPI.Desktop.Controls"
             FlowDirection="RightToLeft" Foreground="{DynamicResource TextPrimaryBrush}">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Border Style="{StaticResource CardBorderStyle}" Width="400" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="32">
            <StackPanel>
                <!-- Brand Icon -->
                <Border Width="50" Height="50" CornerRadius="12" Background="{DynamicResource PrimaryLightBrush}" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <Path Data="{StaticResource IconBrand}" Fill="{DynamicResource PrimaryBrush}" Width="24" Height="24" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>

                <!-- Title -->
                <TextBlock Text="Emerald Management Pro" Style="{StaticResource H1TextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,6" Foreground="{DynamicResource PrimaryBrush}" />
                <TextBlock Text="تسجيل الدخول للنظام وإدارة العمليات" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,24" />

                <!-- Error Message Alert Box -->
                <Border Background="#FEE2E2" BorderBrush="{DynamicResource DangerBrush}" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,14" Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="⚠️ " FontSize="13" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#B91C1C" FontFamily="{StaticResource AppFontFamily}" FontSize="12" FontWeight="Bold" TextWrapping="Wrap" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Email / Username Field -->
                <TextBlock Text="اسم المستخدم أو البريد الإلكتروني" Style="{StaticResource FormLabelStyle}" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Style="{StaticResource DefaultTextBoxStyle}" Margin="0,0,0,16" />

                <!-- Password Field -->
                <TextBlock Text="كلمة المرور" Style="{StaticResource FormLabelStyle}" />
                <PasswordBox x:Name="TxtPassword" Style="{StaticResource DefaultPasswordBoxStyle}" PasswordChanged="TxtPassword_PasswordChanged" Margin="0,0,0,16" />

                <!-- Remember Me Checkbox -->
                <CheckBox Content="تذكرني (حفظ تسجيل الدخول)" IsChecked="{Binding RememberMe}" FontFamily="{StaticResource AppFontFamily}" FontSize="12" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,24" />

                <!-- Login Button -->
                <Button Content="تسجيل الدخول" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding LoginCommand}" Height="42" Margin="0,0,0,12" />
            </StackPanel>
        </Border>

        <controls:LoadingOverlay Grid.RowSpan="1" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
