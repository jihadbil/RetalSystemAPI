<UserControl x:Class="RetalSystemAPI.Desktop.Views.Auth.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:RetalSystemAPI.Desktop.Controls"
             FlowDirection="RightToLeft">
    <Grid Background="{StaticResource BgBrush}">
        <Border Style="{StaticResource CardBorderStyle}" Width="380" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="32">
            <StackPanel>
                <!-- Title -->
                <TextBlock Text="Emerald Management Pro" Style="{StaticResource H1TextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,8" Foreground="{StaticResource PrimaryBrush}" />
                <TextBlock Text="تسجيل الدخول للنظام" Style="{StaticResource CaptionTextBlockStyle}" HorizontalAlignment="Center" Margin="0,0,0,24" />

                <!-- Error Message -->
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" FontFamily="{StaticResource AppFontFamily}" FontSize="13" Margin="0,0,0,12" TextWrapping="Wrap" HorizontalAlignment="Center" Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}" />

                <!-- Email / Username Field -->
                <TextBlock Text="اسم المستخدم أو البريد الإلكتروني" Style="{StaticResource FormLabelStyle}" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DefaultTextBoxStyle}" Margin="0,0,0,16" />

                <!-- Password Field -->
                <TextBlock Text="كلمة المرور" Style="{StaticResource FormLabelStyle}" />
                <PasswordBox x:Name="TxtPassword" Style="{StaticResource DefaultPasswordBoxStyle}" PasswordChanged="TxtPassword_PasswordChanged" Margin="0,0,0,16" />

                <!-- Remember Me Checkbox -->
                <CheckBox Content="تذكرني (حفظ تسجيل الدخول)" IsChecked="{Binding RememberMe}" FontFamily="{StaticResource AppFontFamily}" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,24" />

                <!-- Login Button -->
                <Button Content="تسجيل الدخول" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding LoginCommand}" Height="42" Margin="0,0,0,12" />
            </StackPanel>
        </Border>

        <controls:LoadingOverlay Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
</UserControl>
