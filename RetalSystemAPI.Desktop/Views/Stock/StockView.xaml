<UserControl x:Class="RetalSystemAPI.Desktop.Views.Stock.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <Button Content="تحديث الأرصدة" Command="{Binding RefreshCurrentTabCommand}" DockPanel.Dock="Left" Height="36" Padding="15,0" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            <TextBlock Text="📦 إدارة وتسويه المخزون" FontSize="22" FontWeight="Bold" Foreground="#1F2937" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Warehouse Selector Bar -->
        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="10" Margin="0,0,0,15" BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="المخزن / الصالة المحدد:" VerticalAlignment="Center" Margin="5,0,10,0" FontWeight="SemiBold"/>
                <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name" Width="250" Height="32" VerticalContentAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Tabs -->
        <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex}" Background="White" BorderBrush="#E5E7EB">
            <!-- Tab 1: Storage Stock -->
            <TabItem Header="🏬 مخزون التخزين (بالباركود)" FontSize="14" FontWeight="SemiBold" Padding="15,8">
                <DataGrid ItemsSource="{Binding StorgeStocks}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" HorizontalGridLinesBrush="#F3F4F6" RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="2*"/>
                        <DataGridTextColumn Header="عنصر الباركود" Binding="{Binding BarcodeTitle}" Width="2*"/>
                        <DataGridTextColumn Header="الباركود" Binding="{Binding BarcodeValue}" Width="2*"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="1*"/>
                        <DataGridTextColumn Header="الحد الأدنى" Binding="{Binding MinStockLevel}" Width="1*"/>
                        <DataGridCheckBoxColumn Header="نقص بالمخزون" Binding="{Binding IsBelowMinLevel}" Width="1*"/>
                        <DataGridTemplateColumn Header="تعديل الرصيد" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="تسويه" Command="{Binding DataContext.EditStorgeStockCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#F59E0B" Foreground="White" BorderThickness="0" Padding="8,4" Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Tab 2: Showroom Stock -->
            <TabItem Header="🛍️ مخزون صالة العرض (بالمنتج)" FontSize="14" FontWeight="SemiBold" Padding="15,8">
                <DataGrid ItemsSource="{Binding ShowroomStocks}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" HorizontalGridLinesBrush="#F3F4F6" RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="اسم المنتج" Binding="{Binding ProductName}" Width="3*"/>
                        <DataGridTextColumn Header="الكمية بالصالة" Binding="{Binding Quantity}" Width="1.5*"/>
                        <DataGridTextColumn Header="الحد الأدنى" Binding="{Binding MinStockLevel}" Width="1.5*"/>
                        <DataGridCheckBoxColumn Header="نقص بالمخزون" Binding="{Binding IsBelowMinLevel}" Width="1.5*"/>
                        <DataGridTemplateColumn Header="تعديل الرصيد" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="تسويه" Command="{Binding DataContext.EditShowroomStockCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="#F59E0B" Foreground="White" BorderThickness="0" Padding="8,4" Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Tab 3: Low Stock Alerts -->
            <TabItem Header="⚠️ تنبيهات الرصيد المنخفض" FontSize="14" FontWeight="SemiBold" Padding="15,8">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="10">
                        <TextBlock Text="تنبيهات مخازن التخزين (Storge Alerts)" FontWeight="Bold" Foreground="#DC2626" FontSize="16" Margin="0,5,0,10"/>
                        <DataGrid ItemsSource="{Binding LowStorgeAlerts}" AutoGenerateColumns="False" IsReadOnly="True" Height="180" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="المخزن" Binding="{Binding WarehouseName}" Width="1.5*"/>
                                <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="2*"/>
                                <DataGridTextColumn Header="الباركود" Binding="{Binding BarcodeValue}" Width="1.5*"/>
                                <DataGridTextColumn Header="الكمية الحالية" Binding="{Binding Quantity}" Width="1*"/>
                                <DataGridTextColumn Header="الحد الأدنى" Binding="{Binding MinStockLevel}" Width="1*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock Text="تنبيهات صالات العرض (Showroom Alerts)" FontWeight="Bold" Foreground="#DC2626" FontSize="16" Margin="0,15,0,10"/>
                        <DataGrid ItemsSource="{Binding LowShowroomAlerts}" AutoGenerateColumns="False" IsReadOnly="True" Height="180" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="الصالة" Binding="{Binding WarehouseName}" Width="2*"/>
                                <DataGridTextColumn Header="اسم المنتج" Binding="{Binding ProductName}" Width="3*"/>
                                <DataGridTextColumn Header="الكمية الحالية" Binding="{Binding Quantity}" Width="1.5*"/>
                                <DataGridTextColumn Header="الحد الأدنى" Binding="{Binding MinStockLevel}" Width="1.5*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
