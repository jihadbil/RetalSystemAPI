<Window x:Class="RetalSystemAPI.Desktop.Views.Suppliers.SupplierFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="بيانات المورد والهواتف" Height="500" Width="550" WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="👤 بيانات المورد والهواتف" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextPrimaryBrush}"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="اسم المورد *" FontWeight="SemiBold" Margin="0,5,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" Padding="8,0" VerticalContentAlignment="Center" Background="{DynamicResource SurfaceBrush}"/>

                <TextBlock Text="العنوان" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" Padding="8,0" VerticalContentAlignment="Center" Background="{DynamicResource SurfaceBrush}"/>

                <TextBlock Text="الرصيد الافتتاحي" FontWeight="SemiBold" Margin="0,10,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBox Text="{Binding OpeningBalance, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" Padding="8,0" VerticalContentAlignment="Center" Background="{DynamicResource SurfaceBrush}"/>

                <!-- Supplier Phones Section Card -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,15,0,0" Padding="12" CornerRadius="8" Background="{DynamicResource SurfaceBrush}">
                    <StackPanel>
                        <TextBlock Text="📞 إدارة هواتف المورد" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding NewPhoneNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,5,0" Background="{DynamicResource SurfaceHoverBrush}"/>
                            <TextBox Grid.Column="1" Text="{Binding NewPhoneName, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" VerticalContentAlignment="Center" Padding="8,0" Margin="5,0" Background="{DynamicResource SurfaceHoverBrush}"/>
                            <Button Grid.Column="2" Content="+ إضافة" Command="{Binding AddPhoneCommand}" Height="34" Padding="14,0" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource PrimaryOnBrush}" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                        </Grid>

                        <DataGrid ItemsSource="{Binding Phones}" AutoGenerateColumns="False" IsReadOnly="True" Height="120" HeadersVisibility="Column" Background="{DynamicResource SurfaceBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="رقم الهاتف" Binding="{Binding PhoneNumber}" Width="2*"/>
                                <DataGridTextColumn Header="الاسم / الملاحظات" Binding="{Binding Name}" Width="2*"/>
                                <DataGridTemplateColumn Header="إجراء" Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource TextButtonStyle}" Foreground="{DynamicResource DangerBrush}" Content="حذف" Command="{Binding DataContext.DeletePhoneCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" FontWeight="Bold" BorderThickness="0" Padding="4,2" Cursor="Hand"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource DangerBrush}" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
            <Button Content="حفظ البيانات" Command="{Binding SaveCommand}" Height="36" Padding="20,0" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource PrimaryOnBrush}" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
            <Button Content="إلغاء" IsCancel="True" Height="36" Padding="20,0" Background="{DynamicResource TextMutedBrush}" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
