<UserControl x:Class="RetalSystemAPI.Desktop.Views.Suppliers.SuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" FlowDirection="RightToLeft">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,15">
            <Button Content="+ إضافة مورد جديد" Command="{Binding CreateSupplierCommand}" DockPanel.Dock="Left" Height="36" Padding="15,0" Background="#10B981" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            <TextBlock Text="👥 إدارة الموردين" FontSize="22" FontWeight="Bold" Foreground="#1F2937" VerticalAlignment="Center"/>
        </DockPanel>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="10" Margin="0,0,0,15" BorderBrush="#E5E7EB" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🔍 بحث:" VerticalAlignment="Center" Margin="5,0,10,0" FontWeight="SemiBold" Foreground="#4B5563"/>
                <TextBox Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Height="32" VerticalContentAlignment="Center" Padding="8,0" BorderBrush="#D1D5DB"/>
                <Button Grid.Column="2" Content="تحديث" Command="{Binding LoadSuppliersCommand}" Margin="10,0,0,0" Padding="15,0" Height="32" Background="#3B82F6" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <Border Grid.Row="2" Background="White" CornerRadius="6" BorderBrush="#E5E7EB" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Suppliers}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" HorizontalGridLinesBrush="#F3F4F6" Background="Transparent" RowHeight="40">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="اسم المورد" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="العنوان" Binding="{Binding Address}" Width="3*"/>
                    <DataGridTextColumn Header="الرصيد الافتتاحي" Binding="{Binding OpeningBalance, StringFormat={}{0:N2} د.ل}" Width="1.5*"/>
                    <DataGridTextColumn Header="عدد الهواتف" Binding="{Binding PhoneCount}" Width="1*"/>
                    <DataGridTemplateColumn Header="الإجراءات" Width="180">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="تعديل/هواتف" Command="{Binding DataContext.EditSupplierCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2,0" Padding="8,4" Background="#F59E0B" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                    <Button Content="حذف" Command="{Binding DataContext.DeleteSupplierCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="2,0" Padding="8,4" Background="#EF4444" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Pagination -->
        <DockPanel Grid.Row="3" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="السابق" Command="{Binding PrevPageCommand}" Height="32" Padding="12,0" Margin="0,0,5,0" Background="#E5E7EB" Foreground="#374151" BorderThickness="0"/>
                <TextBlock Text="{Binding CurrentPage}" VerticalAlignment="Center" FontWeight="Bold" Margin="5,0"/>
                <TextBlock Text="من" VerticalAlignment="Center" Margin="2,0"/>
                <TextBlock Text="{Binding TotalPages}" VerticalAlignment="Center" FontWeight="Bold" Margin="5,0"/>
                <Button Content="التالي" Command="{Binding NextPageCommand}" Height="32" Padding="12,0" Margin="5,0,0,0" Background="#E5E7EB" Foreground="#374151" BorderThickness="0"/>
            </StackPanel>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        </DockPanel>
    </Grid>
</UserControl>
