<UserControl x:Class="RetalSystemAPI.Desktop.Views.Pos.PosView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:controls="clr-namespace:RetalSystemAPI.Desktop.Controls"
 FlowDirection="RightToLeft" Background="{DynamicResource BackgroundBrush}" FontFamily="{StaticResource AppFontFamily}"
 PreviewKeyDown="PosView_PreviewKeyDown" Focusable="True" Foreground="{DynamicResource TextPrimaryBrush}">
 <Grid>
 <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
 <Grid Margin="20,16">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,0,0,12">
   <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
    <StackPanel Margin="0,0,12,0">
     <TextBlock Text="امسح الباركود لإضافة صنف" FontWeight="SemiBold" Margin="0,0,0,6"/>
     <TextBox x:Name="BarcodeTextBox" Text="{Binding BarcodeQuery, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="40"
      FlowDirection="LeftToRight" FontSize="17" KeyDown="BarcodeTextBox_KeyDown" AutomationProperties.Name="باركود الصنف"
      ToolTip="F1 للتركيز · Enter لإضافة الصنف"/>
    </StackPanel>
    <WrapPanel Grid.Column="1" VerticalAlignment="Bottom">
     <Button Content="إضافة ↵" Command="{Binding AddBarcodeCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="40" Margin="0,0,8,0"/>
     <Button Content="بحث الأصناف F2" Command="{Binding OpenProductSearchDialogCommand}" Height="40"/>
    </WrapPanel>
    <WrapPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,12,0,0">
     <TextBlock Text="الفرع" VerticalAlignment="Center" Margin="0,0,8,0"/>
     <ComboBox ItemsSource="{Binding Branches}" SelectedItem="{Binding SelectedBranch}" DisplayMemberPath="Name" Width="160" Height="36" Margin="0,0,12,0" AutomationProperties.Name="فرع البيع"/>
     <TextBlock Text="المخزن" VerticalAlignment="Center" Margin="0,0,8,0"/>
     <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name" Width="170" Height="36" Margin="0,0,12,0" AutomationProperties.Name="مخزن البيع"/>
     <Button Content="تحديث" Command="{Binding RefreshCommand}" Height="36" Margin="0,0,8,0"/>
     <Button Content="أصناف سريعة F4" Command="{Binding ToggleQuickItemsCommand}" Height="36"/>
    </WrapPanel>
   </Grid>
  </Border>
  <Border Grid.Row="1" Background="{DynamicResource DangerLightBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,10" Visibility="{Binding ErrorMessage, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}">
   <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap" AutomationProperties.LiveSetting="Assertive"/>
  </Border>
  <Border Grid.Row="2" Background="{DynamicResource SuccessLightBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,10" Visibility="{Binding SuccessMessage, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}">
   <TextBlock Text="{Binding SuccessMessage}" TextWrapping="Wrap" AutomationProperties.LiveSetting="Polite"/>
  </Border>
  <Grid x:Name="SaleLayout" Grid.Row="3" MinHeight="380">
   <Grid.ColumnDefinitions><ColumnDefinition x:Name="CartColumn" Width="*"/><ColumnDefinition x:Name="PaymentColumn" Width="320"/></Grid.ColumnDefinitions>
   <Grid.RowDefinitions><RowDefinition x:Name="CartRow" Height="*"/><RowDefinition x:Name="PaymentRow" Height="0"/></Grid.RowDefinitions>
   <Border x:Name="CartPanel" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,0,14,0">
    <Grid>
     <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <Grid Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <TextBlock Text="{Binding TotalItemCount, StringFormat=إجمالي القطع: {0}}" Grid.Column="1" Margin="12,0,0,0" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
      <TextBlock Text="سلة البيع" FontSize="20" FontWeight="Bold"/>
     </Grid>
     <WrapPanel Grid.Row="1" Margin="0,0,0,10">
      <Button Content="تعليق الطلب F5" Command="{Binding HoldOrderCommand}" Margin="0,0,8,6"/>
      <Button Content="إفراغ السلة" Command="{Binding ClearCartCommand}" Style="{StaticResource TextButtonStyle}" Foreground="{DynamicResource DangerBrush}" Padding="12,8" Margin="0,0,0,6"/>
     </WrapPanel>
     <Grid Grid.Row="2" Margin="0,0,0,10" Visibility="{Binding HasHeldOrder, Converter={StaticResource BoolToVisibilityConverter}}">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <ComboBox ItemsSource="{Binding HeldOrders}" SelectedItem="{Binding SelectedHeldOrder}" DisplayMemberPath="Label" AutomationProperties.Name="الطلبات المعلقة" Margin="0,0,8,0"/>
      <Button Grid.Column="1" Content="استعادة F6" Command="{Binding RestoreHeldOrderCommand}"/>
     </Grid>
     <Grid Grid.Row="3">
      <ListBox ItemsSource="{Binding CartItems}" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
       ScrollViewer.VerticalScrollBarVisibility="Auto" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
       <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/><Setter Property="Padding" Value="0"/><Setter Property="Margin" Value="0,0,0,6"/></Style></ListBox.ItemContainerStyle>
       <ListBox.ItemTemplate><DataTemplate>
        <Border Background="{DynamicResource SurfaceHoverBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="10">
         <Grid>
          <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
          <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <StackPanel Margin="0,0,10,8">
           <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" TextWrapping="Wrap"/>
           <TextBlock Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Text="{Binding UnitPrice, StringFormat=سعر الوحدة: {0:N2} د.ل}"/>
          </StackPanel>
          <TextBlock Grid.Column="1" Text="{Binding LineTotal, StringFormat={}{0:N2} د.ل}" FontSize="17" FontWeight="Bold"/>
          <StackPanel Grid.Row="1" Orientation="Horizontal">
           <Button Content="−" Width="36" Height="34" Padding="0" Command="{Binding DataContext.DecrementQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" AutomationProperties.Name="تقليل الكمية"/>
           <Button Content="{Binding Quantity}" MinWidth="48" Height="34" Padding="8,0" Margin="6,0" Click="QuantityButton_Click" Tag="{Binding}" AutomationProperties.Name="تعديل الكمية" Cursor="Hand" ToolTip="انقر لفتح لوحة الأرقام السريعة"/>
           <Button Content="+" Width="36" Height="34" Padding="0" Command="{Binding DataContext.IncrementQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" AutomationProperties.Name="زيادة الكمية"/>
           <TextBlock Text="{Binding UnitName}" VerticalAlignment="Center" Margin="10,0" Foreground="{DynamicResource TextMutedBrush}"/>
          </StackPanel>
          <Button Grid.Row="1" Grid.Column="1" Content="حذف" Style="{StaticResource TextButtonStyle}" Foreground="{DynamicResource DangerBrush}"
           Command="{Binding DataContext.RemoveCartItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" AutomationProperties.Name="حذف الصنف من السلة"/>
         </Grid>
        </Border>
       </DataTemplate></ListBox.ItemTemplate>
      </ListBox>
      <TextBlock Text="السلة فارغة&#x0a;امسح باركودًا أو ابحث عن صنف للبدء." TextAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center" HorizontalAlignment="Center">
       <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding CartItems.Count}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
      </TextBlock>
     </Grid>
     <Expander Grid.Row="4" Header="أصناف سريعة" IsExpanded="{Binding IsQuickItemsVisible}" Margin="0,8,0,0" Foreground="{DynamicResource TextPrimaryBrush}">
      <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
       <ItemsControl ItemsSource="{Binding QuickItems}">
        <ItemsControl.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate><DataTemplate>
         <Button Width="125" Height="60" Margin="0,6,6,0" Padding="8" Command="{Binding DataContext.AddQuickItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
          <StackPanel><TextBlock Text="{Binding Name}" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding Price, StringFormat={}{0:N2} د.ل}" FontWeight="SemiBold" FontSize="13"/></StackPanel>
         </Button>
        </DataTemplate></ItemsControl.ItemTemplate>
       </ItemsControl>
      </ScrollViewer>
     </Expander>
     <TextBlock Grid.Row="5" Text="{Binding DraftStatus}" FontSize="12" TextWrapping="Wrap" Foreground="{DynamicResource TextMutedBrush}" Margin="0,10,0,0"/>
     <TextBlock Grid.Row="5" Text="جارٍ إضافة الأصناف الممسوحة…" FontSize="12" Margin="0,10,0,0" Background="{DynamicResource SurfaceBrush}" Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityConverter}}"/>
    </Grid>
   </Border>
   <Border x:Name="PaymentPanel" Grid.Column="1" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="18">
    <Grid>
     <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <StackPanel Margin="0,0,0,16">
      <TextBlock Text="الإجمالي المستحق" Foreground="{DynamicResource TextMutedBrush}"/>
      <TextBlock Text="{Binding GrandTotal, StringFormat={}{0:N2} د.ل}" FontSize="30" FontWeight="Bold" Margin="0,4,0,0"/>
      <TextBlock Text="{Binding SubTotal, StringFormat=قبل الخصم والضريبة: {0:N2} د.ل}" Foreground="{DynamicResource TextMutedBrush}" FontSize="12"/>
<StackPanel x:Name="CashSummary" Margin="0,8,0,0">        <Border Background="{DynamicResource PrimaryLightBrush}" CornerRadius="8" Padding="10" Margin="0,0,0,10">
         <StackPanel><TextBlock Text="الباقي للعميل" FontSize="13"/><TextBlock Text="{Binding ChangeAmount, StringFormat={}{0:N2} د.ل}" FontSize="22" FontWeight="Bold"/></StackPanel>
        </Border>
        <TextBlock Text="{Binding PaymentShortfall, StringFormat=نقص الدفع: {0:N2} د.ل}" Foreground="{DynamicResource DangerBrush}" Margin="0,0,0,10" Visibility="{Binding PaymentShortfall, Converter={StaticResource NumberToVisibilityConverter}}"/>
</StackPanel>
     </StackPanel>
     <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel Margin="0,0,4,0">
       <TextBlock Text="طريقة الدفع" FontWeight="SemiBold" Margin="0,0,0,8"/>
       <UniformGrid Columns="3" Margin="0,0,0,16">
        <RadioButton x:Name="PayCashRadio" Content="نقدي" GroupName="Payment" Checked="PayCash_Checked"/>
        <RadioButton x:Name="PayCardRadio" Content="بطاقة" GroupName="Payment" Checked="PayCard_Checked" ToolTip="F9"/>
        <RadioButton x:Name="PayCreditRadio" Content="آجل" GroupName="Payment" Checked="PayCredit_Checked"/>
       </UniformGrid>
       <StackPanel x:Name="CashFields">
        <TextBlock Text="المبلغ المستلم" Margin="0,0,0,6"/>
        <TextBox Text="{Binding PaidAmount, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="42" FontSize="20" FlowDirection="LeftToRight" AutomationProperties.Name="المبلغ المستلم"/>
        <Button Content="المبلغ كاملًا F8" Command="{Binding SetQuickCashCommand}" CommandParameter="0" Margin="0,8,0,8"/>
        <UniformGrid Columns="4" Margin="0,0,0,12">
         <Button Content="20" Command="{Binding SetQuickCashCommand}" CommandParameter="20" Padding="6" Margin="2"/>
         <Button Content="50" Command="{Binding SetQuickCashCommand}" CommandParameter="50" Padding="6" Margin="2"/>
         <Button Content="100" Command="{Binding SetQuickCashCommand}" CommandParameter="100" Padding="6" Margin="2"/>
         <Button Content="200" Command="{Binding SetQuickCashCommand}" CommandParameter="200" Padding="6" Margin="2"/>
        </UniformGrid>
       </StackPanel>
       <TextBlock x:Name="CardHint" Text="سيُسجل الإجمالي كاملًا كدفعة بالبطاقة." TextWrapping="Wrap" Margin="0,0,0,12"/>
       <StackPanel x:Name="CreditFields">
        <TextBlock Text="الدفعة الحالية (اختياري)" Margin="0,0,0,6"/>
        <TextBox Text="{Binding PaidAmount, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="40" FlowDirection="LeftToRight" AutomationProperties.Name="الدفعة الحالية"/>
        <TextBlock Text="{Binding CustomerDebtAmount, StringFormat=المتبقي على الحساب: {0:N2} د.ل}" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,8,0,12"/>
        <TextBlock Text="{Binding SelectedCustomer.CurrentBalance, StringFormat=رصيد العميل: {0:N2} د.ل}" FontSize="12" Margin="0,0,0,8"/>
       </StackPanel>
       <TextBlock Text="العميل (مطلوب للبيع الآجل)" FontSize="13" Margin="0,0,0,6"/>
       <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" DisplayMemberPath="Name" IsEditable="True" IsTextSearchEnabled="True" Height="38" AutomationProperties.Name="عميل الفاتورة"/>
       <Button Content="إضافة عميل F3" Command="{Binding OpenQuickCustomerDialogCommand}" Style="{StaticResource TextButtonStyle}" Margin="0,6,0,14" HorizontalAlignment="Left"/>
       <Expander Header="الخصم والضريبة والملاحظات" Foreground="{DynamicResource TextPrimaryBrush}">
        <StackPanel Margin="0,10,0,12">
         <TextBlock Text="الخصم" Margin="0,0,0,6"/>
         <TextBox Text="{Binding OrderDiscount, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="38" AutomationProperties.Name="خصم الفاتورة"/>
         <TextBlock Text="الضريبة" Margin="0,10,0,6"/>
         <TextBox Text="{Binding TaxAmount, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="38" AutomationProperties.Name="ضريبة الفاتورة"/>
         <TextBlock Text="ملاحظات" Margin="0,10,0,6"/>
         <TextBox Text="{Binding OrderNotes, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" MinHeight="50" TextWrapping="Wrap" AcceptsReturn="True" AutomationProperties.Name="ملاحظات الفاتورة"/>
        </StackPanel>
       </Expander>
       <TextBlock Text="{Binding OrderDiscount, StringFormat=الخصم المطبق: {0:N2} د.ل}" FontSize="12" Margin="0,6,0,0" Visibility="{Binding OrderDiscount, Converter={StaticResource NumberToVisibilityConverter}}"/>
       <TextBlock Text="{Binding TaxAmount, StringFormat=الضريبة: {0:N2} د.ل}" FontSize="12" Margin="0,6,0,0" Visibility="{Binding TaxAmount, Converter={StaticResource NumberToVisibilityConverter}}"/>
      </StackPanel>
     </ScrollViewer>
     <Button Grid.Row="2" Content="إتمام البيع F10" Command="{Binding SubmitSaleCommand}" Style="{StaticResource PrimaryButtonStyle}" FontSize="18" Height="52" Margin="0,16,0,0" ToolTip="حفظ الفاتورة · F10"/>
    </Grid>
   </Border>
  </Grid>
 </Grid>
 </ScrollViewer>
  <controls:LoadingOverlay Grid.RowSpan="1" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

  <!-- Floating Quantity Numpad Popup -->
  <Popup x:Name="QuantityPopup" StaysOpen="False" AllowsTransparency="True" PopupAnimation="Fade" Placement="Bottom">
      <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1.5" CornerRadius="12" Padding="14" Width="280">
          <Border.Effect>
              <DropShadowEffect BlurRadius="20" Direction="270" ShadowDepth="6" Opacity="0.25" Color="Black"/>
          </Border.Effect>
          <Grid>
              <Grid.RowDefinitions>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
                  <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>

              <!-- Item title & Unit Price -->
              <DockPanel Grid.Row="0" Margin="0,0,0,8">
                  <TextBlock x:Name="PopupItemName" Text="الصنف" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                  <TextBlock x:Name="PopupUnitPrice" Text="0.00 د.ل" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
              </DockPanel>

              <!-- Quantity Display / Entry Box with - and + -->
              <Border Grid.Row="1" Background="{DynamicResource PrimaryLightBrush}" CornerRadius="8" Padding="4" Margin="0,0,0,8" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1.5">
                  <Grid>
                      <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="Auto"/>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="Auto"/>
                      </Grid.ColumnDefinitions>
                      <Button Grid.Column="0" Content="−" Click="PopupMinus_Click" Width="34" Height="34" Style="{StaticResource GhostButtonStyle}" FontSize="18" FontWeight="Bold"/>
                      <TextBox Grid.Column="1" x:Name="PopupQuantityText" Text="1" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryDarkBrush}" TextAlignment="Center" VerticalContentAlignment="Center" Background="Transparent" BorderThickness="0" KeyDown="PopupQuantity_KeyDown"/>
                      <Button Grid.Column="2" Content="+" Click="PopupPlus_Click" Width="34" Height="34" Style="{StaticResource GhostButtonStyle}" FontSize="18" FontWeight="Bold"/>
                  </Grid>
              </Border>

              <!-- Numpad Grid -->
              <UniformGrid Grid.Row="2" Rows="4" Columns="3" Margin="0,0,0,8">
                  <Button Content="7" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="8" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="9" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>

                  <Button Content="4" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="5" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="6" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>

                  <Button Content="1" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="2" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="3" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>

                  <Button Content="C" Click="PopupClear_Click" Margin="2" Height="34" FontSize="14" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Foreground="{DynamicResource DangerBrush}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="0" Click="PopupDigit_Click" Margin="2" Height="34" FontSize="15" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
                  <Button Content="⌫" Click="PopupBackspace_Click" Margin="2" Height="34" FontSize="14" FontWeight="Bold" Style="{StaticResource GhostButtonStyle}" Background="{DynamicResource BackgroundBrush}"/>
              </UniformGrid>

              <!-- Confirm Button -->
              <Button Grid.Row="3" Content="تأكيد الكمية ↵" Click="PopupConfirm_Click" Style="{StaticResource PrimaryButtonStyle}" Height="34" FontWeight="Bold" FontSize="13"/>
          </Grid>
      </Border>
  </Popup>
  </Grid>
</UserControl>
