<Window x:Class="RetalSystemAPI.Desktop.Views.Purchase.PurchaseInvoiceFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="فاتورة مشتريات بضاعة" Height="680" Width="920" 
        WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft" Background="#F9FAFB">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ════════ HEADER ════════ -->
        <TextBlock Grid.Row="0" Text="📥 تسجيل فاتورة مشتريات جديدة" Style="{StaticResource H2TextBlockStyle}" Margin="0,0,0,16"/>

        <!-- ════════ INVOICE MAIN FIELDS ════════ -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="10" Padding="16" Margin="0,0,0,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Invoice Number -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                    <TextBlock Text="رقم الفاتورة *" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding InvoiceNumber, UpdateSourceTrigger=PropertyChanged}" Height="34" VerticalContentAlignment="Center" Padding="6,0"/>
                </StackPanel>

                <!-- Branch -->
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
                    <TextBlock Text="الفرع المستلم *" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Branches}" SelectedItem="{Binding SelectedBranch}" DisplayMemberPath="Name" Height="34" VerticalContentAlignment="Center"/>
                </StackPanel>

                <!-- Supplier -->
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,10,10">
                    <TextBlock Text="المورد *" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" Height="34" VerticalContentAlignment="Center"/>
                </StackPanel>

                <!-- Warehouse -->
                <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,0,10">
                    <TextBlock Text="المستودع/الصالة المستلمة *" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name" Height="34" VerticalContentAlignment="Center"/>
                </StackPanel>

                <!-- Payment Method -->
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="طريقة الدفع" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <ComboBox SelectedValue="{Binding PaymentMethod}" SelectedValuePath="Tag" Height="34" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="💵 نقداً" Tag="{x:Static sales:PaymentMethod.Cash}" IsSelected="True" xmlns:sales="clr-namespace:RetalSystemAPI.Desktop.Models.Sales"/>
                        <ComboBoxItem Content="💳 بطاقة" Tag="{x:Static sales:PaymentMethod.Card}" xmlns:sales="clr-namespace:RetalSystemAPI.Desktop.Models.Sales"/>
                        <ComboBoxItem Content="📑 آجل / دين" Tag="{x:Static sales:PaymentMethod.Credit}" xmlns:sales="clr-namespace:RetalSystemAPI.Desktop.Models.Sales"/>
                    </ComboBox>
                </StackPanel>

                <!-- Status -->
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="حالة الفاتورة" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <ComboBox SelectedValue="{Binding Status}" SelectedValuePath="Tag" Height="34" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="✅ مدفوعة بالكامل" Tag="{x:Static sales:InvoiceStatus.Paid}" IsSelected="True" xmlns:sales="clr-namespace:RetalSystemAPI.Desktop.Models.Sales"/>
                        <ComboBoxItem Content="⏳ مسددة جزئياً" Tag="{x:Static sales:InvoiceStatus.PartiallyPaid}" xmlns:sales="clr-namespace:RetalSystemAPI.Desktop.Models.Sales"/>
                        <ComboBoxItem Content="📑 صادرة (آجل)" Tag="{x:Static sales:InvoiceStatus.Issued}" xmlns:sales="clr-namespace:RetalSystemAPI.Desktop.Models.Sales"/>
                    </ComboBox>
                </StackPanel>

                <!-- Date -->
                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="تاريخ الفاتورة" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding InvoiceDate}" Height="34"/>
                </StackPanel>

                <!-- Notes -->
                <StackPanel Grid.Row="1" Grid.Column="3">
                    <TextBlock Text="ملاحظات" Style="{StaticResource CaptionTextBlockStyle}" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Height="34" VerticalContentAlignment="Center" Padding="6,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ════════ ADD ITEM ROW ════════ -->
        <Border Grid.Row="2" Background="#ECFDF5" CornerRadius="10" Padding="14" Margin="0,0,0,12" BorderBrush="#38E07B" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.5*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="اختر الصنف *" FontWeight="SemiBold" Foreground="#065F46" Margin="0,0,0,4"/>
                    <ComboBox ItemsSource="{Binding AvailableProducts}" SelectedItem="{Binding SelectedProductToAdd}" DisplayMemberPath="Name" Height="34" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,8,0">
                    <TextBlock Text="الكمية *" FontWeight="SemiBold" Foreground="#065F46" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding QuantityToAdd, UpdateSourceTrigger=PropertyChanged}" Height="34" VerticalContentAlignment="Center" Padding="6,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,8,0">
                    <TextBlock Text="سعر التكلفة (د.ل) *" FontWeight="SemiBold" Foreground="#065F46" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding UnitPriceToAdd, UpdateSourceTrigger=PropertyChanged}" Height="34" VerticalContentAlignment="Center" Padding="6,0"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="0,0,8,0">
                    <TextBlock Text="الخصم (د.ل)" FontWeight="SemiBold" Foreground="#065F46" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding ItemDiscountToAdd, UpdateSourceTrigger=PropertyChanged}" Height="34" VerticalContentAlignment="Center" Padding="6,0"/>
                </StackPanel>

                <Button Grid.Column="4" Content="+ إضافة للبند" Command="{Binding AddItemCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="34" Padding="16,0" VerticalAlignment="Bottom" FontWeight="Bold"/>
            </Grid>
        </Border>

        <!-- ════════ ITEMS DATA GRID ════════ -->
        <Border Grid.Row="3" Background="#FFFFFF" CornerRadius="10" Padding="8" Margin="0,0,0,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid ItemsSource="{Binding Items}" CanUserAddRows="False" IsReadOnly="True" AutoGenerateColumns="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3F4F6" RowHeight="38">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="اسم الصنف" Binding="{Binding ProductName}" Width="2*" FontWeight="Bold"/>
                    <DataGridTextColumn Header="الباركود" Binding="{Binding BarCode}" Width="1.2*"/>
                    <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity}" Width="1*"/>
                    <DataGridTextColumn Header="سعر الشراء" Binding="{Binding UnitPrice, StringFormat={}{0:N2} د.ل}" Width="1.2*"/>
                    <DataGridTextColumn Header="الخصم" Binding="{Binding DiscountAmount, StringFormat={}{0:N2} د.ل}" Width="1*"/>
                    <DataGridTextColumn Header="إجمالي البند" Binding="{Binding LineTotal, StringFormat={}{0:N2} د.ل}" Width="1.3*" FontWeight="Bold" Foreground="#059669"/>
                    <DataGridTemplateColumn Width="0.8*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="✕" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Foreground="#DC2626" Background="Transparent" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ════════ TOTALS & FINANCIAL SUMMARY ════════ -->
        <Border Grid.Row="4" Background="#F3F4F6" CornerRadius="10" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="المجموع الفرعي" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock Text="{Binding SubTotal, StringFormat={}{0:N2} د.ل}" FontSize="15" FontWeight="Bold" Foreground="#111827"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="الخصم الإجمالي" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBox Text="{Binding DiscountAmount, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="28" HorizontalAlignment="Left" VerticalContentAlignment="Center" Padding="4,0" Background="#FFFFFF"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="الصافي المطلوب" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N2} د.ل}" FontSize="18" FontWeight="Bold" Foreground="#059669"/>
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="المدفوع" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBox Text="{Binding PaidAmount, UpdateSourceTrigger=PropertyChanged}" Width="100" Height="28" HorizontalAlignment="Left" VerticalContentAlignment="Center" Padding="4,0" Background="#FFFFFF" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="المتبقي (الدين)" Style="{StaticResource CaptionTextBlockStyle}"/>
                    <TextBlock Text="{Binding RemainingAmount, StringFormat={}{0:N2} د.ل}" FontSize="16" FontWeight="Bold" Foreground="#DC2626"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ════════ ACTIONS & ERROR ════════ -->
        <DockPanel Grid.Row="5">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#EF4444" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="إلغاء" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButtonStyle}" Height="38" Padding="20,0" Margin="0,0,8,0"/>
                <Button Content="💾 حفظ فاتورة المشتريات وزيادة المخزون" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButtonStyle}" Height="38" Padding="24,0" FontWeight="Bold"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
