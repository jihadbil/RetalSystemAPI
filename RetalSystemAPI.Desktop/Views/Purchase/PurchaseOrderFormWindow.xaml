<Window x:Class="RetalSystemAPI.Desktop.Views.Purchase.PurchaseOrderFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        Title="تفاصيل أمر الشراء / الطلبية" Height="620" Width="720" WindowStartupLocation="CenterOwner" FlowDirection="RightToLeft" Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📝 تفاصيل أمر الشراء / الطلبية" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="{DynamicResource TextPrimaryBrush}"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,5,0">
                        <TextBlock Text="رقم الطلبية *" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBox Text="{Binding OrderNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" Padding="8,0" VerticalContentAlignment="Center" Background="{DynamicResource SurfaceBrush}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="المورد (إلزامي عند الاستلام)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBox ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}" DisplayMemberPath="Name" Height="34" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,5,0">
                        <TextBlock Text="الفرع *" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBox ItemsSource="{Binding Branches}" SelectedItem="{Binding SelectedBranch}" DisplayMemberPath="Name" Height="34" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,5,0">
                        <TextBlock Text="المخزن / الصالة" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <ComboBox ItemsSource="{Binding Warehouses}" SelectedItem="{Binding SelectedWarehouse}" DisplayMemberPath="Name" Height="34" VerticalContentAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2" Margin="5,0,0,0">
                        <TextBlock Text="التاريخ المتوقع للاستلام" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <DatePicker SelectedDate="{Binding ExpectedDate}" Height="34" VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Items Section Card -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,10,0,0" Padding="12" CornerRadius="8" Background="{DynamicResource SurfaceBrush}">
                    <StackPanel>
                        <TextBlock Text="📦 بنود الطلبية والمشتريات" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="14"/>

                        <!-- Add Item Controls Bar -->
                        <Border Background="{DynamicResource SurfaceHoverBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="2*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Search Input -->
                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="🔍 ابحث بالاسم أو الباركود *" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,3"/>
                                        <TextBox Text="{Binding ItemSearchQuery, UpdateSourceTrigger=PropertyChanged, Delay=300}" Height="34" Padding="8,0" VerticalContentAlignment="Center" Background="{DynamicResource SurfaceBrush}"/>
                                        <CheckBox Content="مطابقة الباركود تماماً" IsChecked="{Binding ExactBarcodeMatch}" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,4,0,0" ToolTip="يقتصر البحث على التطابق التام مع رقم الباركود"/>
                                    </StackPanel>

                                    <!-- Filtered Results Dropdown -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="الأصناف المطابقة للبحث *" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,3"/>
                                        <ComboBox ItemsSource="{Binding FilteredBarcodeOptions}" SelectedItem="{Binding SelectedBarcodeOption}" DisplayMemberPath="DisplayText" Height="34" VerticalContentAlignment="Center"/>
                                    </StackPanel>
                                </Grid>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                        <TextBlock Text="الكمية *" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,3"/>
                                        <TextBox Text="{Binding InputQuantity, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" Padding="6,0" VerticalContentAlignment="Center" TextAlignment="Center" Background="{DynamicResource SurfaceBrush}"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Margin="0,0,8,0">
                                        <TextBlock Text="سعر الشراء للوحدة *" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,0,3"/>
                                        <TextBox Text="{Binding InputUnitPrice, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Height="34" Padding="6,0" VerticalContentAlignment="Center" TextAlignment="Center" Background="{DynamicResource SurfaceBrush}"/>
                                    </StackPanel>

                                    <Button Grid.Column="2" Content="+ إضافة بند للطلبية" Command="{Binding AddItemCommand}" Height="34" Padding="15,0" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource PrimaryOnBrush}" FontWeight="Bold" BorderThickness="0" Cursor="Hand" VerticalAlignment="Bottom"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="False" MinHeight="140" MaxHeight="220" HeadersVisibility="Column" Background="{DynamicResource SurfaceBrush}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="اسم المنتج" Binding="{Binding ProductName}" Width="2*"/>
                                <DataGridTextColumn Header="الباركود / النكهة" Binding="{Binding BarcodeValue}" Width="1.5*"/>
                                <DataGridTextColumn Header="الكمية" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="1*"/>
                                <DataGridTextColumn Header="سعر الوحدة" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="1*"/>
                                <DataGridTextColumn Header="الإجمالي" Binding="{Binding LineTotal, StringFormat={}{0:N2}}" IsReadOnly="True" Width="1.2*"/>
                                <DataGridTemplateColumn Header="إجراء" Width="60">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource TextButtonStyle}" Foreground="{DynamicResource DangerBrush}" Content="حذف" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" FontWeight="Bold" BorderThickness="0" Padding="4,2"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <DockPanel Margin="0,10,0,0">
                            <TextBlock Text="إجمالي التكلفة الكلية:" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalAmount, StringFormat={}{0:N2} د.ل}" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource PrimaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </DockPanel>
                    </StackPanel>
                </Border>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource DangerBrush}" Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
            <Button Content="حفظ الطلبية" Command="{Binding SaveCommand}" Height="36" Padding="20,0" Background="{DynamicResource PrimaryBrush}" Foreground="{DynamicResource PrimaryOnBrush}" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"/>
            <Button Content="إلغاء" IsCancel="True" Height="36" Padding="20,0" Background="{DynamicResource TextMutedBrush}" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
