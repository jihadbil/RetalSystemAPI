<UserControl x:Class="RetalSystemAPI.Desktop.Controls.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#80FFFFFF">
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="160" Height="6" Foreground="{StaticResource PrimaryBrush}" />
                <TextBlock Text="جاري التحميل..." FontFamily="{StaticResource AppFontFamily}" FontSize="14" Foreground="{StaticResource TextMutedBrush}" Margin="0,12,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
