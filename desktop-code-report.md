# تقرير تحليل وتقييم كود طبقة سطح المكتب

**المشروع:** RetalSystemAPI.Desktop  
**تاريخ التحليل:** 2026-09-11  
**النطاق:** منطق التشغيل في مشروع سطح المكتب فقط: التهيئة والحقن، المصادقة والجلسة، عميل HTTP، خدمات الـAPI، النماذج، وViewModels وتدفقات العمل.  
**المستبعد صراحةً:** ملفات XAML، التصميم، الأنماط والألوان، الصور، تقييم تجربة الاستخدام، وملفات الواجهات الخلفية `*.xaml.cs`. لم تُعرض أو تُفحص أي صورة.  
**ملاحظة:** التقرير مبني على الحالة الحالية لمجلد العمل، والذي يحتوي على تعديلات محلية كثيرة غير مثبتة في Git.

## الملخص التنفيذي

الطبقة منظمة مبدئياً حول MVVM، وتستخدم حقن الاعتماديات وخدمات API ذات واجهات واضحة، كما أن تجميع المشروع ينجح دون أخطاء أو تحذيرات. توجد أيضاً نقاط جيدة في استمرارية مسودات نقطة البيع، وتوحيد استدعاءات HTTP، ومنع تكرار فاتورة POS عند إعادة المحاولة.

لكن النسخة الحالية غير جاهزة للإنتاج. أخطر العيوب هي تعطيل التحقق من شهادة TLS بالكامل، واعتماد صلاحيات سطح المكتب على قاعدة تسمح افتراضياً لأي مستخدم غير مصنف كـCashier بدخول الوظائف الإدارية، وأخطاء نقطة البيع التي قد تخصم باركوداً غير صحيح وتسجل تكلفة الصنف بصفر، وتدفق فاتورة المشتريات الذي ينشئ فاتورة ويؤثر في المخزون منذ إضافة أول بند ثم يتركها قائمة عند إغلاق النموذج دون حفظ نهائي.

**التقييم العام: 4.5 / 10 — بنية أولية جيدة، لكن الأمان وصحة التدفقات المالية والمخزنية تمنع النشر الإنتاجي.**

| المحور | التقييم | الملاحظة المختصرة |
|---|---:|---|
| تنظيم المشروع وMVVM | 7/10 | فصل جيد للمجلدات واستخدام مناسب لـCommunityToolkit.Mvvm وDI |
| أمان الاتصال والجلسة | 2/10 | قبول أي شهادة TLS وخلل في `RememberMe` وتفويض عميل غير آمن |
| صحة منطق المبيعات وPOS | 3/10 | دمج باركودات مختلفة وإرسال `UnitCost = 0` وانتقالات حالة خطرة |
| صحة منطق المشتريات والمخزون | 2/10 | الحفظ الفوري والإلغاء المحلي قد يتركان فاتورة ومخزوناً غير مقصودين |
| اتساق عقود الـAPI | 4/10 | DTOs وEnums مكررة يدوياً وبدأ بينها وبين الخادم انحراف فعلي |
| معالجة الأخطاء والإلغاء | 5/10 | نقطة مركزية جيدة، لكن الأخطاء والإلغاء والتنزيلات تفقد معلومات مهمة |
| القابلية للاختبار والصيانة | 5/10 | خدمات قابلة للاستبدال، لكن ViewModels ضخمة ومقترنة بالنوافذ والملفات |

## نطاق الفحص والتحقق

- 71 ملف C# في جانب منطق سطح المكتب بعد استبعاد `Views` و`Controls` و`Resources` وملفات `*.xaml.cs`.
- 21 ملف ViewModel، و20 ملف خدمة، و19 ملف نماذج.
- مراجعة `App.xaml.cs`، و`Core/Auth`، و`Core/Http`، وخدمات الـAPI، ونماذج العقود، وتدفقات POS والمبيعات والمشتريات والمخزون والاستيراد.
- أكبر ملفات منطق العمل حالياً:
  - `CatalogViewModels.cs`: نحو 44 KB.
  - `PurchaseInvoicesViewModel.cs`: نحو 41 KB.
  - `PosViewModel.cs`: نحو 29 KB.
  - `ProductImportWizardViewModel.cs`: نحو 26 KB.
- تم بناء المشروع إلى مجلد مؤقت معزول بسبب وجود نسخة من التطبيق مفتوحة تقفل ملف التنفيذ في مجلد `bin`:

```text
dotnet build RetalSystemAPI.Desktop/RetalSystemAPI.Desktop.csproj --no-restore -o <temporary-output>
Build succeeded.
0 Warning(s)
0 Error(s)
```

- يوجد مشروع `tests/RetalSystemAPI.Desktop.UxChecks`، لكنه برنامج Console مخصص وليس مشروع xUnit/NUnit/MSTest، ويخلط فحوص منطق POS مع توليد لقطات مرئية. لم يتم تشغيله لأن التقييم الحالي يستبعد الواجهة والصور.

## النتائج الحرجة (P0)

### 1. تعطيل التحقق من شهادة HTTPS بالكامل

يسجل `App.xaml.cs:89-92` معالج HTTP يعيد `true` دائماً من `ServerCertificateCustomValidationCallback`. هذا يجعل التطبيق يقبل أي شهادة، بما فيها الشهادات المزورة أو المنتهية، ويمكن أن يسمح باعتراض JWT والبيانات المالية وتعديل الردود أثناء النقل.

كما أن عنوان الخادم مثبت داخل الكود على `https://localhost:7226/api/` في `App.xaml.cs:87`، فلا يوجد فصل سليم بين إعداد التطوير والإنتاج.

**الإصلاح:** حذف تجاوز التحقق من الشهادة واستخدام التحقق الافتراضي. يوضع `BaseAddress` في إعدادات بيئية، وإذا كانت شهادة التطوير الذاتية ضرورية فيكون الاستثناء محصوراً في Debug وبالتحقق من بصمة شهادة محددة، لا بإرجاع `true` العام.

### 2. التفويض في التطبيق يسمح افتراضياً بالوظائف الإدارية

في `ShellViewModel.cs:75-76` يتم حساب:

```text
IsCashier = auth.IsCashier
CanAccessAdmin = !auth.IsCashier
```

أي أن المستخدم الذي لا يملك أدواراً، أو يملك دوراً جديداً/غير معروف، يعامل كمستخدم إداري. كذلك لا تمنع دوال التنقل Cashier إلا من المستأجرين وأوامر وفواتير ومرتجعات الشراء والمستخدمين (`ShellViewModel.cs:147-304`)، بينما تظل مجالات الفروع والأصناف والموردين والمخازن والمخزون والتحويلات والتسويات والمبيعات متاحة له.

إخفاء أو إظهار الوظائف في العميل ليس حاجزاً أمنياً، لكن المشكلة تصبح قابلة للاستغلال فعلياً لأن طبقة المتحكمات الحالية تستخدم `[Authorize]` العام دون سياسات أدوار، كما وُثق في `controllers-report.md`.

**الإصلاح:** اعتماد نموذج منع افتراضي `deny by default` وقائمة قدرات صريحة تأتي من الخادم، مثل `CanManageUsers` و`CanAdjustStock`. يجب أن يطبق الخادم السياسات نفسها بوصفه مصدر الحماية الحقيقي.

### 3. نقطة البيع قد تخصم المخزون من الباركود الخطأ وتحسب الربح خطأ

يوجد عيبان مستقلان في `PosViewModel`:

1. عند إضافة منتج إلى السلة، البحث عن السطر الموجود يتم بواسطة `ProductId` فقط في `PosViewModel.cs:424-430`. إذا مسح المستخدم باركودين/نكهتين مختلفتين لنفس المنتج، يزيد السطر الأول وتبقى قيمة `ProductBarCodeId` الخاصة بالباركود الأول. النتيجة المحتملة هي خصم كمية الصنف من متغير/باركود غير صحيح.
2. الاختصار السريع يمرر نص الباركود لكنه لا يمرر معرفه في `PosViewModel.cs:354-360`، بينما طلب الحفظ يعتمد على `ProductBarCodeId` في `PosViewModel.cs:736-743`. قد يختار الخادم باركوداً افتراضياً بدلاً من المتغير الذي اختاره المستخدم.
3. إنشاء بنود الفاتورة في `PosViewModel.cs:736-744` لا يعين `UnitCost`. الخاصية رقمية وقيمتها الافتراضية صفر في `SalesInvoiceModels.cs:134-143`، والخادم يحفظها كما وردت في `SalesInvoiceService.cs:198`. تقارير الربح تطرح `Quantity * UnitCost` في `DashboardService.cs:63`، ولذلك تظهر المبيعات ذات التكلفة الصفرية كأن كامل إيرادها ربح.

العيب الثالث موجود أيضاً في نموذج فاتورة المبيعات العادية: الإنشاء في `SalesInvoicesViewModel.cs:465-473` لا يعين `UnitCost`، وتحميل فاتورة قائمة في `SalesInvoicesViewModel.cs:426-435` يسقط التكلفة الموجودة ثم يرسل صفراً عند التحديث.

**الإصلاح:** يكون مفتاح دمج السلة `(ProductId, ProductBarCodeId)`، وتحمل الاختصارات معرف الباركود لا النص فقط. والأهم أن تكلفة البيع لا ينبغي الوثوق بها من العميل أصلاً؛ يجب على خدمة الخادم اشتقاقها من المخزون/سياسة التكلفة داخل معاملة الحفظ.

### 4. تدفق فاتورة المشتريات يترك آثاراً دائمة عند مجرد إغلاق النموذج

عند إضافة أول بند، ينشئ `PurchaseInvoicesViewModel` فاتورة `Pending` فوراً على الخادم في `PurchaseInvoicesViewModel.cs:865-901`. خدمة الخادم تزيد المخزون وتحدث التكلفة فور الإنشاء بصرف النظر عن كون الحالة Pending، وذلك في `PurchaseInvoiceService.cs:150-156`.

بعد ذلك يتم إرسال تحديث عند كل إضافة وحذف (`PurchaseInvoicesViewModel.cs:903-929` و`932-974`). لكن أمر الإلغاء في `PurchaseInvoicesViewModel.cs:1104-1105` يغلق التدفق محلياً فقط ولا يستدعي `CancelAsync`. لذلك يمكن للمستخدم أن يضيف أول بند ثم يغلق، فتظل فاتورة Pending ويظل أثرها في المخزون والتكلفة.

يوجد خطر إضافي عند الحذف: العميل يستطيع حذف أي عنصر من المجموعة في `PurchaseInvoicesViewModel.cs:941-945`، بينما خدمة الخادم تستنتج المحذوف حسب فرق العدد وتحذف البنود الأخيرة في `PurchaseInvoiceService.cs:235-256`. حذف بند من منتصف القائمة قد يبقي البند المطلوب حذفه ويحذف بنداً آخر ويعكس مخزونه.

**الإصلاح الموصى به:** إبقاء المسودة محلية حتى الضغط على حفظ، ثم إرسال أمر واحد ذري للخادم. إذا كان الحفظ التدريجي مطلباً، فيلزم endpoint صريح للبند بالمعرف مع optimistic concurrency، وإلغاء حقيقي يعكس الأثر داخل معاملة. لا يجوز تحديد الإضافة أو الحذف بموقع العنصر أو فرق العدد.

## النتائج عالية الخطورة (P1)

### 5. خيار «تذكرني» يحذف الرمز المحفوظ بعد أول تشغيل لاحق

يحمل المنشئ الرمز من Windows Credential Manager ثم يستدعي `ProcessToken(savedToken, saveToStore: false)` في `AuthStateService.cs:22-30`. داخل `ProcessToken` يؤدي `saveToStore == false` إلى حذف الرمز من المخزن في `AuthStateService.cs:141-150`.

النتيجة: الرمز المحفوظ ينجح في تشغيل واحد لاحق، لكنه يحذف أثناء هذا التشغيل ولا يبقى للتشغيل الذي يليه.

**الإصلاح:** فصل قرار «الحفظ بعد تسجيل الدخول» عن قرار «حذف بيانات الاعتماد». تحميل رمز محفوظ يجب ألا يحذفه. يمكن استخدام دالتين واضحتين: `RestoreSavedToken()` و`SetToken(token, persistenceMode)`.

### 6. تحليل JWT محلياً دون تحقق وتدفق انتهاء غير مكتمل

يفك `AuthStateService.cs:50-140` حمولة JWT يدوياً ويستخدم Claims لتحديد المستخدم والمستأجر والأدوار. هذا التحليل لا يتحقق من التوقيع أو المصدر أو الجمهور. كما يقبل رمزاً لا يحتوي `exp`، ولا توجد آلية تراقب الانتهاء أثناء بقاء التطبيق مفتوحاً.

يمكن استخدام Claims المفكوكة لتحسين سلوك العميل فقط، لا لإثبات الصلاحية. الرد 401 يمسح الجلسة مركزياً في `ApiClient.cs:226-231` و`266-271`، لكنه يحدث بعد أول طلب فاشل.

**الإصلاح:** التحقق الفعلي يبقى في الخادم، ويعتمد العميل على استجابة مصادقة موثوقة أو endpoint للهوية/القدرات. يضاف مؤقت انتهاء أو refresh-token flow، مع عدم قبول رمز بلا `exp` إن كان ذلك مخالفاً للعقد.

### 7. انتقال حالة فاتورة المبيعات قد يعيد فاتورة مغلقة إلى حالة نشطة

`SalesInvoicesViewModel.cs:170-179` يختار الحالة التالية هكذا: Draft إلى Issued، وIssued إلى Paid، وأي حالة أخرى إلى Issued. وبذلك يمكن للأمر نفسه أن يحاول تحويل Paid أو Cancelled أو Voided إلى Issued.

هذا ليس مجرد نص عرض؛ الدالة ترسل التحديث فعلاً إلى الخادم في `SalesInvoicesViewModel.cs:181-184`، والخادم الحالي لا يفرض آلة حالات صارمة لجميع الانتقالات.

**الإصلاح:** تعريف transitions صريحة مشتركة مع الخادم، وتعطيل الأمر للحالات النهائية. الخادم يجب أن يرفض أي انتقال غير مسموح مهما أرسله العميل.

### 8. عقود سطح المكتب مكررة وحدث بينها وبين عقود الخادم انحراف

يعرّف سطح المكتب DTOs وEnums مستقلة يدوياً. أمثلة فعلية:

- `InvoiceStatus` في `SalesInvoiceModels.cs:6-14` يعرّف `Pending = 1` و`Issued = 1` كاسمين للقيمة نفسها، بينما الخادم يعرف `Pending = 1` فقط.
- `PaymentMethod` في `SalesInvoiceModels.cs:17-26` يضيف `Multiple = 6`، بينما enum الخادم في `RetalSystemAPI.Models/Enums/PaymentMethod.cs:3-10` ينتهي عند القيمة 5.
- `CreditCard = 3` و`Card = 3` اسمان محليان لنفس القيمة، ما يزيد الغموض في العرض والتسلسل.

قد يؤدي إرسال قيمة 6 إلى الخادم إلى قيمة غير مدعومة إذا لم توجد validation صريحة للأعداد المعرفة في enum.

**الإصلاح:** إنشاء عميل وDTOs من OpenAPI، أو نقل العقود المشتركة الخالية من تبعيات الخادم إلى مشروع Contract مشترك. يضاف اختبار contract يمنع أي فرق في أسماء وقيم enums والحقول المطلوبة.

### 9. عميل HTTP يخفي أنواعاً مهمة من الفشل

التجميع المركزي داخل `ApiClient` نقطة جيدة، لكن التنفيذ الحالي لديه مشكلات:

- معظم الدوال تمسك `Exception` وتعيد `NETWORK_ERROR` مع `ex.Message` (`ApiClient.cs:78-220`)، فتختلط مهلة الطلب وإلغاء المستخدم وأخطاء البرمجة وفشل الشبكة.
- `DownloadFileAsync` في `ApiClient.cs:45-59` يعيد `null` لكل فشل، ولا يمسح الجلسة عند 401، ولا يحافظ على status code أو رسالة الخادم.
- التنزيل يقرأ الملف كله إلى الذاكرة عبر `ReadAsByteArrayAsync` في `ApiClient.cs:53`.
- لا توجد سياسة retry/backoff للطلبات الآمنة، ولا correlation ID، ولا logging منظم، ولا إعداد timeout واضح.
- بعض رسائل الخطأ تكشف نص الاستثناء التقني مباشرة للمستخدم.

**الإصلاح:** نتيجة موحدة تحمل status/error code وكون العملية ألغيت، ومعالجة منفصلة لـ`OperationCanceledException`، واستخدام streaming للتنزيلات الكبيرة. يسجل التفصيل تقنياً ويعرض للمستخدم نصاً آمناً مختصراً.

### 10. العمليات غير المتزامنة كثيرة بلا إلغاء مرتبط بدورة حياة الشاشة

توجد عشرات الاستدعاءات من نوع `_ = Load...Async()` داخل المنشئات ومعالجات تغيير الخصائص في معظم ViewModels. لا يوجد استخدام ظاهر لـ`CancellationTokenSource` في ViewModels، رغم أن خدمات API تقبل `CancellationToken`.

`BaseViewModel.DebounceSearchAsync` ينتظر 300ms دون إلغاء في `BaseViewModel.cs:23-29`، و`ExecuteAsync` يمسك كل الاستثناءات في `BaseViewModel.cs:38-57`. هذا قد يترك طلبات قديمة تعمل بعد تغيير المرشح أو مغادرة التدفق، وقد يجعل نتيجة أقدم تستبدل نتيجة أحدث في ViewModels التي لا تطبق version check خاصاً بها.

**الإصلاح:** CancellationToken خاص بكل تحميل/بحث، وإلغاؤه عند بدء طلب جديد وعند التخلص من ViewModel. يمنع fire-and-forget غير المراقب أو يمر عبر helper يسجل الاستثناء ويراعي دورة الحياة.

### 11. توليد أرقام المستندات في العميل قابل للتصادم

تستخدم عدة تدفقات دقة الثانية فقط:

- فاتورة البيع: `SalesInvoicesViewModel.cs:362`.
- فاتورة الشراء: `PurchaseInvoicesViewModel.cs:306`.
- مرتجع البيع: `SalesReturnsViewModel.cs:313`.
- مرتجع الشراء: `PurchaseReturnsViewModel.cs:272` و`358`.
- التسوية: `StockAdjustmentsViewModel.cs:348`.
- التحويل: `StockTransfersViewModel.cs:371`.

عميلان أو عمليتان في الثانية نفسها قد ينتجان الرقم نفسه، فيفشل الحفظ بسبب uniqueness أو يحدث سلوك إعادة محاولة غير مقصود. رقم POS أفضل لأنه مشتق من GUID ثابت للمسودة في `PosViewModel.cs:716-718`.

**الإصلاح:** توليد الرقم في الخادم ضمن sequence آمن لكل مستأجر/فرع، وإرجاعه للعميل. يبقى GUID منفصل كمفتاح idempotency.

### 12. حد الائتمان يعرض تحذيراً ولا يطبق قاعدة عمل

`PosViewModel.cs:700-706` يحسب تجاوز `CreditLimit` ويعرض تحذيراً، لكنه يكمل إنشاء الفاتورة. وبحسب تحليل طبقة الخدمات، الخادم لا يفرض الحد بصورة حاسمة أيضاً.

**الإصلاح:** إذا كان الحد قاعدة إلزامية فيجب رفض العملية في الخادم، مع صلاحية override موثقة للمدير وسبب مسجل. وإذا كان مجرد تنبيه فيجب تسميته كذلك وعدم اعتباره حماية.

## النتائج المتوسطة (P2)

### 13. دالة تصدير الأسطر المرفوضة غير مكتملة

`CatalogApiServices.cs:175-179` يستدعي endpoint يعيد ملفاً ثنائياً باستخدام `PostAsync<object>` ثم يعيد `null` دائماً. لا يظهر استخدام حالي لهذه الدالة، لكنها عقد مكسور وقد يفشل بصمت إذا استدعيت لاحقاً.

**الإصلاح:** حذفها إن كانت مهجورة، أو إضافة `PostForBytesAsync` يعالج status والأخطاء والمحتوى الثنائي بصورة صريحة، ثم تغطيتها باختبار.

### 14. ViewModels كبيرة وتمزج منطق العمل مع الوصول المباشر للنظام

تجاوزت بعض الملفات 25–44 KB، وتجمع التحميل والتحقق والحساب والتحويل بين DTOs وإدارة الملفات وإظهار رسائل النظام في صنف واحد. `CatalogViewModels.cs` و`ProductImportWizardViewModel.cs` يستخدمان file dialogs و`MessageBox` مباشرة، ما يجعل اختبار منطق الاستيراد والتصدير دون WPF أصعب.

**الإصلاح:** استخراج use-case services مثل `IProductImportWorkflow` و`IInvoiceEditor` و`IDocumentNumberProvider`، وتجريد اختيار الملفات والتنبيهات خلف واجهات صغيرة. يبقى ViewModel منسقاً للحالة والأوامر فقط.

### 15. مسودات POS مخزنة كنص JSON وتنفذ I/O متزامناً دورياً

`PosSessionService.cs:19-40` يخزن المسودات في `LocalApplicationData` باسم مشتق بـSHA-256 من نطاق المستخدم، ويكتب ملف JSON مؤقتاً ثم ينقله فوق الملف الأصلي. الكتابة الذرية بهذه الطريقة نقطة جيدة، وكذلك فصل الملفات حسب tenant/user.

لكن المحتوى نفسه غير مشفر، وقد يتضمن اسم العميل والبنود والأسعار والملاحظات. كما أن `PosViewModel.cs:35` يشغل مؤقت حفظ كل 350ms، وخدمة التخزين تستخدم `File.WriteAllText` المتزامن؛ بطء القرص قد يحجب خيط الواجهة. والنسخ العميق يتم عبر serialize/deserialize كاملاً في `PosDraft.cs:30`.

**الإصلاح:** تشفير المسودة بـDPAPI عند اعتبارها بيانات حساسة، واعتماد حفظ غير متزامن متسلسل مع debounce أطول وكتابة فقط عند تغير الحالة، وتنظيف المسودات القديمة بسياسة retention واضحة.

### 16. تخزين الاعتماد آمن نسبياً لكن فشله صامت

استخدام Windows Credential Manager في `CredentialStoreService.cs:11-46` أفضل من ملف نصي. لكن كل فشل في الحفظ أو القراءة أو الحذف يذهب إلى `Debug.WriteLine` فقط، فيستطيع المستخدم تفعيل «تذكرني» بينما يفشل الحفظ دون أي إشارة أو telemetry.

**الإصلاح:** إرجاع نتيجة من خدمة التخزين وتسجيل الخطأ عبر `ILogger`، ثم إبلاغ المستخدم برسالة مناسبة إذا تعذر حفظ الجلسة.

### 17. الاعتماد على حزم Preview في مشروع أعمال

يستهدف المشروع `net10.0-windows` ويستخدم `Microsoft.Extensions.DependencyInjection` و`Microsoft.Extensions.Http` بالإصدار `10.0.0-preview.1.25080.5` في ملف المشروع، بينما بقية الحزم مستقرة.

**الإصلاح:** تثبيت إصدارات مستقرة ومتوافقة مع إطار التشغيل المستهدف قبل الإصدار الإنتاجي، وتفعيل تحديثات الاعتماديات وفحص الثغرات بصورة دورية.

## نقاط جيدة ينبغي الحفاظ عليها

- تقسيم خدمات الـAPI إلى واجهات وتنفيذات حسب المجال يسهل الاختبار والاستبدال.
- وجود `ApiClient` مركزي لإضافة Bearer token وتحليل غلاف الاستجابة يقلل التكرار ويعطي موضعاً واحداً لتحسين الأخطاء.
- معالجة `ValidationProblemDetails` داخل عميل HTTP دفاع جيد أمام عدم اتساق بعض ردود الخادم.
- استخدام Windows Credential Manager بدلاً من تخزين JWT في إعدادات أو ملف عادي.
- MVVM Toolkit يقلل boilerplate، و`ObservableValidator` في `BaseViewModel` أساس جيد للتحقق.
- رقم فاتورة POS ثابت لكل مسودة ويقلل خطر إنشاء فاتورتين عند ضياع الرد وإعادة المحاولة.
- مسودات POS مفصولة حسب tenant/user وتكتب عبر ملف مؤقت ثم نقل overwrite، ما يقلل فساد الملف عند انقطاع الكتابة.
- منطق POS يتحقق من الكميات والمدفوعات ويحد `PaidAmount` من تجاوز الإجمالي في مسار الحفظ.
- المشروع ينجح في البناء بـ0 تحذيرات و0 أخطاء في المخرج المعزول.

## خطة الإصلاح المقترحة

### المرحلة الأولى — مانع النشر

1. إزالة تجاوز TLS ونقل عنوان الـAPI إلى إعدادات بيئية.
2. تطبيق Policies وصلاحيات الخادم، ثم تحويل العميل إلى `deny by default` بناءً على capabilities موثوقة.
3. جعل تكلفة البيع مشتقة من الخادم، وإصلاح مفتاح سطر POS ليشمل الباركود.
4. إعادة تصميم تدفق فاتورة المشتريات بحيث لا يؤثر مجرد فتح/إغلاق النموذج في المخزون.
5. منع انتقالات الحالات غير القانونية في الخادم والعميل.

### المرحلة الثانية — سلامة العقود والجلسة

1. إصلاح `RememberMe` وإضافة تدفق انتهاء/تجديد واضح.
2. توحيد عقود العميل والخادم عبر OpenAPI أو مشروع Contracts مشترك.
3. توليد أرقام المستندات في الخادم وإضافة idempotency keys للإنشاءات المالية.
4. توحيد أخطاء HTTP، والإلغاء، والـtimeouts، والتنزيل المتدفق.

### المرحلة الثالثة — الصيانة والاختبارات

1. تقسيم ViewModels الكبيرة إلى workflows وخدمات حساب وتحويل مستقلة.
2. تمرير CancellationToken وإلغاء الطلبات القديمة بدلاً من fire-and-forget غير المراقب.
3. إضافة logging منظم مع correlation IDs وعدم عرض الاستثناء الخام للمستخدم.
4. تحويل الفحوص المنطقية إلى مشروع اختبارات قياسي منفصل عن أي فحوص مرئية.

## الاختبارات المطلوبة قبل الإنتاج

### POS والمبيعات

- مسح باركودين مختلفين للمنتج نفسه والتأكد من بقائهما سطرين ومن خصم المخزون الصحيح.
- إنشاء فاتورة من الاختصار السريع والتأكد من إرسال `ProductBarCodeId` الصحيح.
- التأكد من أن `UnitCost` يأتي من الخادم ولا يمكن للعميل تزويره أو تصفيره.
- إعادة محاولة الحفظ بعد timeout والتأكد من إنشاء فاتورة واحدة فقط.
- رفض كل انتقال حالة غير مسموح، خصوصاً Cancelled/Voided/Paid إلى Issued.
- رفض تجاوز حد الائتمان أو تسجيل override مصرح به.

### المشتريات

- إضافة بند ثم إغلاق التدفق دون حفظ، والتأكد من عدم بقاء فاتورة أو أثر مخزني.
- حذف البند الأول أو الأوسط والتأكد من حذف البند نفسه وعكس مخزونه فقط.
- تعديل كمية بند قائم والتأكد من مزامنة فرق المخزون والتكلفة.
- فشل منتصف العملية والتأكد من rollback كامل للفاتورة والمخزون والتكلفة.

### الأمان والجلسة

- رفض شهادة غير موثوقة أو باسم مضيف غير مطابق.
- تشغيل التطبيق عدة مرات بعد اختيار «تذكرني» والتأكد من بقاء الجلسة حتى انتهاء الرمز أو تسجيل الخروج.
- مستخدم بلا أدوار أو بدور غير معروف لا يحصل على أي قدرة إدارية.
- انتهاء JWT أثناء فتح التطبيق يؤدي إلى تسجيل خروج منظم دون إبقاء عمليات معلقة.

### العقود والتزامن

- اختبار آلي يقارن قيم enums والحقول المطلوبة بين OpenAPI وعميل سطح المكتب.
- إنشاء مستندين في اللحظة نفسها ومن عميلين مختلفين دون تصادم أرقام.
- تغيير المرشح سريعاً والتأكد من أن الرد الأقدم لا يستبدل البيانات الأحدث.
- إلغاء عملية تنزيل/استيراد والتأكد من تمييز الإلغاء عن فشل الشبكة وتنظيف الملفات المؤقتة.

## الخلاصة

الأساس الهندسي قابل للتحسين ولا يحتاج إلى إعادة كتابة كاملة: DI، والخدمات ذات الواجهات، وMVVM، وعميل HTTP المركزي كلها اختيارات مناسبة. الأولوية ليست إضافة مزيد من الوظائف، بل تثبيت حدود الثقة وصحة العمليات المالية: TLS، والتفويض، وتكلفة البيع، وهوية الباركود، وذرية المشتريات وانتقالات الحالات. بعد إغلاق P0 وP1 وإضافة اختبارات تكامل لهذه التدفقات يمكن رفع التقييم بصورة كبيرة.
