# تقرير التحليل المعمق لطبقة الخدمات (Services Layer)

**المشروع:** RetalSystemAPI
**تاريخ التحليل:** 2026-09-11
**النطاق:** مشروع `RetalSystemAPI.Services` بالكامل، مع فحص حدودي لطبقة DataAccess (UnitOfWork، Repository، AppDbContext، Configurations) وطبقة DTOs وControllers حيثما يلزم لفهم سلوك الخدمات.
**الحالة التقنية:** المشروع يُبنى بنجاح — 0 أخطاء، 0 تحذيرات (net10.0).
**ملاحظة:** يوجد تقرير سابق باسم `services-report.md`؛ هذا التقرير تحليل مستقل جديد ومبني على قراءة سطر-بسطر للخدمات والبنية التحتية المحيطة.

---

## 1. الملخص التنفيذي

طبقة الخدمات **مبنية معماريًا بشكل جيد** (فصل واضح للوحدات، واجهات لكل خدمة، نمط `ServiceResult` موحد، Specifications لتغليف الاستعلامات، عزل مستأجرين عبر Query Filters)، لكنها تعاني من **ثغرات جوهرية في صحة منطق الأعمال** — خصوصًا في العمليات المالية والمخزنية — ومن **سطح أمني غير مكتمل** في نقاط حساسة.

| المحور | التقييم | خلاصة |
|---|---:|---|
| التنظيم المعماري وفصل المسؤوليات | 7.5/10 | تقسيم ممتاز حسب المجالات مع أنماط موحدة |
| صحة منطق الأعمال (الفواتير/المخزون) | 3.5/10 | ثغرات حسابية وتحكم العميل في الإجماليات |
| سلامة البيانات والمعاملات | 3/10 | Check-Then-Act بلا أقفال، تحديثات مفهرسة هشة |
| الأمان (مصادقة/تفويض/ملفات) | 4/10 | سر JWT احتياطي مضمّن، تفويض Controllers جيد |
| الأداء وقابلية التوسع | 4/10 | N+1 منتشر، تحميل جداول كاملة في الذاكرة |
| القابلية للاختبار والصيانة | 5/10 | واجهات ممتازة، لكن صفر اختبارات وصفر تسجيل |

**الحكم العام: غير جاهزة للإنتاج بوضعها الحالي** قبل معالجة بنود P0 (القسم 6). البنية الأساسية سليمة لدرجة أن أغلب المعالجات ممكنة دون إعادة هيكلة كبيرة.

---

## 2. البنية العامة للمشروع

### 2.1 الإحصائيات

- **~10,208 سطرًا** موزعة على **66 ملف C#**
- **24 خدمة** + **24 واجهة** + **14 ملف Specifications** + نماذج مشتركة (`ServiceResult`, `PagedResult`, `ErrorCodes`) + ملف تسجيل DI واحد
- أضخم ملف: `ProductExcelService.cs` (**1,645 سطرًا** — God Class)
- أضخم الخدمات: `ProductExcelService` (1645)، `UserService` (628)، `SalesInvoiceService` (526)، `PurchaseInvoiceService` (493)

### 2.2 التقنيات والتبعيات

- `net10.0` مع `Nullable` و`ImplicitUsings` مفعّلين
- **AutoMapper 13.0.1** + **ClosedXML 0.105.1** (للإكسل)
- `FrameworkReference: Microsoft.AspNetCore.App` — يعني أن طبقة الخدمات مرتبطة بـ ASP.NET Core (لأجل `IFormFile`, `IWebHostEnvironment`, Identity) — هذا **تسريب استضافة (Hosting Leak) إلى طبقة يُفترض أن تكون مجردة**، ويعيق إعادة استخدام الخدمات من تطبيق Desktop/MAUI دون واجهة ويب.
- مراجع: `DataAccess` و`Models.DTOs`

### 2.3 تنظيم الوحدات (Vertical Slices)

كل مجال أعمال يُدار داخل مجلد خاص به بثلاث عشريات ثابتة:

```
Services/
├── Auth/          (Interfaces | Implementations)
├── Branch/        (+ Specifications)
├── Catalog/       7 خدمات (Product, Category, Unit, ProductUnit, BarCode, Image, Excel)
├── Customers/
├── Dashboard/     (حقن AppDbContext مباشرة!)
├── FileUpload/
├── Purchase/      3 خدمات (Order, Invoice, Return)
├── Sales/         خدمتان (Invoice, Return)
├── Suppliers/
├── Tenant/
├── Users/         (حقن AppDbContext مباشرة!)
├── Warehouses/    4 خدمات (Warehouse, Stock, Transfer, Adjustment)
├── Common/Models/ (ServiceResult, PagedResult, ErrorCodes)
└── Extensions/    (ServicesLayerExtensions — نقطة تسجيل DI الوحيدة)
```

**الإيجابي:** الاتساق الكامل — أي مطور يستطيع التنبؤ بمكان أي شيء.
**السلبب:** التسجيل مركزي في `ServicesLayerExtensions.cs:55-82` بـ 22 سطر `AddScoped` يدوي — سيتضخم مع كل خدمة جديدة، ولا تحقق تلقائي أن كل Interface مسجّل (نمط Scrutor أو تسجيل بالتجميع كان أنظف). لاحظ أيضًا السطر 82: `UserService` مسجّل بأسماء مؤهلة بالكامل بخلاف بقية السطور — مؤشر ترقيع متأخر.

---

## 3. الأنماط المعمارية المتبعة

### 3.1 نمط النتيجة الموحد `ServiceResult<T>`

`Common/Models/ServiceResult.cs` — غلاف ممتاز: `IsSuccess`, `Data`, `ErrorMessage`, `ErrorCode`، مع مصنعين `Success/Failure` وخصائص `private set`. **مطبق ب100% عبر كل الخدمات** — التزام نادر ومحمود. مكوّن `ErrorCodes.cs` (101 كود) يتيح للواجهات الأمامية التعامل البرمجي مع الأخطاء.

**عيب:** لا يحمل قائمة أخطاء متعددة (فقط رسالة واحدة نصية)، لذا أخطاء التحقق المتعددة تُدمج في سطر واحد (كما في `AuthService.cs:81-83`). غلاف Result متعدد الأخطاء (مثل `FluentResults` أو `ServiceResult<T, TError>`) كان سيخدم سيناريو الاستيراد من Excel بشكل أفضل.

### 3.2 نمط المواصفات (Specification Pattern)

14 ملف Specifications تغلف Include/Filter/OrderBy. مثال `SalesInvoiceWithDetailsSpec` — ثلاثة constructors (id / number / filters).

**مشاكل رصدتها:**
1. **Include بنصوص سحرية:** `AddInclude("Items.Product")` (SalesInvoiceSpecifications.cs:19-20) — أي إعادة تسمية خاصية تنكسر صامتًا وقت التشغيل لا وقت البناء. تعبيرات `Expression<Func<...>>` كانت ستكشفها وقت التصريف.
2. **constructor الفلترة بمعاملات اختيارية موضعية** (8 معاملات،SalesInvoiceSpecifications.cs:47-65) — الخدمات تمررها موضعيًا؛ إعادة ترتيب = خطأ صامت.
3. **Overfetching للقوائم الملخصة:** `GetAllAsync`/`GetPagedAsync` تستخدمان نفس الـ Spec "بالتفاصيل الكاملة" (5 Includes) ثم تُحوَّل إلى `SummaryDto` — أي أن الشبكة (Grid) تسحب بنود الفاتورة والباركودات والعملاء رغم أن الـ Summary لا يحتاجها إلا لاسم واحد. على فاتورة بـ 50 بندًا هذا تضخم بيانات ملموس.

### 3.3 وحدة العمل (Unit of Work) والمعاملات

`UnitOfWork` يوفر 32 مستودعًا مؤجلًا (Lazy) + `BeginTransactionAsync/Commit/Rollback` بتنفيذ سليم (مع Rollback داخل catch في Commit).

**المفارقة الحرجة:** البنية موجودة لكنها **غير مستخدمة في أخطر المواضع**:

| الخدمة | عملية مالية/مخزنية | معاملة صريحة؟ |
|---|---|---|
| SalesInvoiceService (Create/Update/Cancel/Delete) | خصم/استرجاع مخزون + فاتورة | ❌ |
| PurchaseInvoiceService (Create/Update/Cancel/Delete) | زيادة/عكس مخزون + تكاليف | ❌ |
| PurchaseReturnService.CreateAsync | خصم مخزون + مرتجع | ❌ |
| SalesReturnService.CreateAsync | زيادة مخزون + مرتجع | ❌ |
| StockTransferService.UpdateStatusAsync | خصم مصدر + إضافة وجهة | ❌ |
| StockAdjustmentService.CreateAsync | تعديل أرصدة + تسوية | ❌ |
| PurchaseOrderService.UpdateStatusAsync (Received) | مخزون + فاتورة مولدة آليًا | ❌ |
| ProductExcelService.ImportProducts | استيراد شامل | ✅ |
| ProductUnitService.SetDefaultAsync | تحديث بنود | ✅ |
| ProductImageService | ترتيب الصور | ✅ |

منح الدرجة: الخدمات الثلاث التي تستخدم المعاملات هي الأقل حساسية ماليًا، بينما كل دورة المستندات (Sales/Purchase Cycle) بلا معاملات. سيُقال إن `SaveChangesAsync` واحد ذري — لكن ذريته تنكسر فعليًا في نمط "عدّل الكيانات المتتبعة ثم احفظ" عندما يفشل الحفظ بـ `DbUpdateConcurrencyException` (سياق RowVersion أدناه): التغييرات تبقى عالقة في ChangeTracker وقد تُحفظ لاحقًا في طلب آخر بنفس الـ Scoped instance إذا استمر الاستثناء دون Dispose... والأهم: **لا يوجد أي التقاط لـ DbUpdateConcurrencyException في هذه الخدمات** — الطلبات المتزامنة ستنتج 500 غير معالج، مع احتمال خصم خاطئ قبل الفشل.

### 3.4 مستودع البيانات (Repository) — تتبع الكيانات

قراءة `Repository.cs` كشفت فخًا منتشرًا:
- `FirstOrDefaultAsync` (سطر 80-90) → **AsNoTracking** (غير متتبع)
- `FirstOrDefaultTrackedAsync` (سطر 95-106) → متتبع

عدة خدمات تقع في النمط: تجلب بـ **غير متتبع** → تعدّل → `Update()` — و`Update` على كيان غير متتبع يعلّمه **كاملًا** كـ Modified، أي:
1. **يستبدل كل الأعمدة** بما فيها القيم القديمة المقروءة (يفسد أي تحديث متزامن لحقل آخر — Last-Write-Wins كامل الصف).
2. **RowVersion قديم** → DbUpdateConcurrencyException (غير ملتقط).

أمثلة: `SalesInvoiceService.UpdateStatusAsync` (سطر 428: `FirstOrDefaultAsync` ثم `Update`)، `StockTransferService.UpdateAsync` (سطر 153)، `PurchaseInvoiceService.UpdateAsync` (سطر 167). بينما الخدمات الأخرى تستخدم `FirstOrDefaultTrackedAsync` بشكل صحيح (`SalesInvoiceService.UpdateAsync:245`, `DeleteAsync:483`). **عدم اتساق يفرض نفسه كمراجعة منقّحة.**

### 3.5 العزل متعدد المستأجرين (Multi-Tenancy)

البنية ممتازة:
- `AppDbContext.cs:140-190`: Query Filters عامة تُبنى بالتعبيرات ديناميكيًا: `!IsDeleted` + `TenantId == CurrentTenantId` لكل `TenantBaseEntity`، و`!IsDeleted` لغيرها.
- `AppDbContext.cs:203-227` + `AuditInterceptor`: تعبئة `TenantId` تلقائيًا عند الإضافة، وحقول التدقيق (`CreatedAt/UpdatedAt`).
- `CurrentTenantService` يستخرج `TenantId` من JWT claims.
- الفهارس الفريدة مركّبة بالمستأجر: `SalesInvoiceConfiguration.cs:78` (`TenantId + InvoiceNumber`) — فريدة، وهذا يعني أن فحص `ExistsAsync` في الخدمة هو فقط للرسالة اللطيفة؛ العيار الأخير هو الفهرس (وسيظهر كـ 500 غير ملتقط عند السباق).

**ثغرة عزل:** الخدمة التي تتجاوز الفلاتر بقصد: `TenantService.PopulateTenantStatsAsync` (TenantService.cs:168-173) يستخدم `IgnoreQueryFilters` عمدًا — مشروع لمنطق الإحصاء العابر للمستأجرين لكنه الشاذ الوحيد، وكان يكفي أن يبقى داخل نفس الطبقة بمستودع مخصص بدل حقن `AppDbContext` مباشرة (انظر 3.6).

**ثغرة ثانية:** `FileUploadService.cs:58` — عند غياب TenantId يكتب في مجلد `"global"` — سلوك مقبول للـ seeding لكن يستحق توثيقًا.

### 3.6 انتهاك التوجه الواحد: حقن `AppDbContext` مباشرة

النمط الموحد هو `IUnitOfWork + IMapper`، لكن ثلاث خدمات تحقن `AppDbContext` مباشرة:
- `DashboardService` (السبب: استعلامات GroupBy معقدة — قرار مفهوم لكن كان يجب توظيفه عبر Repository/Query مخصص)
- `UserService` (بسبب Identity)
- `TenantService` (بسبب IgnoreQueryFilters)

هذا يقوّض الغرض من طبقة المستودعات ويجعل الاستعلامات الخام قابلة للتسلل. **التوصية:** توسيع `IRepository` بـ `IQueryable<T> Query()` موثّق أو إضافة مستودعات قراءة متخصصة.

---

## 4. تحليل الوحدات الوظيفية

### 4.1 المصادقة — `AuthService` (243 سطرًا)

**ما يمتاز:**
- منطق الصلاحيات في التوكن (سطر 154-215) ناضج: Admin/SuperAdmin يحصلان على كل الأكواد؛ مستخدم "مخصص" (`Permissions.Customized`) يتجاوز ورثة الدور؛ غير المخصص يرث ادوارًا + إضافات مباشرة. يتطابق التنفيذ مع `UserService.GetUserPermissionsAsync` — اتساق ممتاز بين توليد التوكن وقراءة الصلاحيات.

**الثغرات:**
1. **[P0-أمن] سر JWT احتياطي مضمّن بالشيفرة** (AuthService.cs:124): `"DefaultSuperSecretKeyForRetalSystemAPI123456789!"` — إذا غاب `JwtSettings:Secret` من الإعدادات، يوقّع النظام بتوقيع معروف للعامة = **أي شخص يقدر أن يزوّر توكن Admin**. يجب أن يكون غياب السر فشل بدء صارم (`Fail Fast`) وليس سقوطًا صامتًا لقيمة مضمّنة.
2. **[P1] `LogoutAsync` عملية وهمية** (سطر 112-115): يعيد Success دون فعل شيء، و`RefreshToken` يُعاد دائمًا `string.Empty` — API سطحي مضلل: لا قائمة إبطال (Revocation)، والتوكن يبقى صالحًا 8 ساعات بعد "تسجيل الخروج". إما تنفيذ قائمة سوداء (أو مرجع إصدار Token Version في الـ Claims) أو إزاحة الواجهة من العقد.
3. **[P1] لا حماية من القفل/التخمين:** `LoginAsync` يستخدم `CheckPasswordAsync` دون `lockoutOnFailure` ولا فحص `LockoutEnd` — هجمات القاموس غير مقيّدة. `RegisterAsync` يكشف وجود اسم المستخدم/البريد برسائل صريحة (User Enumeration) — مقبول داخليًا، خطر خارجيًا.
4. **[P2] `ExpiryHours` قابل للسلاسل:** `int.TryParse` يمرر أي قيمة (سالب/صفر) بلا تدقيق مدى.

### 4.2 المستخدمون والأدوار — `UserService` (628 سطرًا)

- **[P1] N+1 في الترحيل:** `GetPagedUsersAsync` (سطر 83-86) يستدعي `GetRolesAsync` لكل مستخدم داخل حلقة — صفحة 50 مستخدم = 50 استعلام إضافي.
- **[P2] أثر كتابي في قراءة:** `GetRolesAsync` (سطر 350-357) **ينشئ الأدوار الافتراضية إذا لم توجد عند كل استدعاء** — قراءة تُغيّر قاعدة البيانات (يفسد بيئات القراءة فقط/النسخ المتماثلة).
- **[P1] `UpdateUserAsync` لا يتحقق من فرادة البريد** (سطر 250): `user.Email = dto.Email` مباشرة دون فحص تكرار عبر `UserManager` validators — Identity قد يرفض لاحقًا أو يقبل حسب الإعداد.
- **جيد:** كل العمليات مفلترة بالمستأجر (`u.TenantId == tenantId`) — عزل سليم. `ResetPasswordAsync` عبر توليد Token فوري واستخدامه — النمط القياسي.
- **[P2] `DeleteUserAsync` حذف فعلي (Hard)** عبر `UserManager.DeleteAsync` — بينما النظام كله Soft-Delete — المستخدم المحذوف يفقد تاريخه نهائيًا وأي `CreatedByUserId` المرتبط يصبح يتيمًا.

### 4.3 فواتير المبيعات — `SalesInvoiceService` (526 سطرًا)

الأهم في النظام لأنه **نقطة الخروج النقدي**. التدفقات: Show (صالة عرض → رصيد على مستوى Product) وStorge (مخزن → رصيد على مستوى Barcode/نكهة).

**الثغرات الحرجة:**
1. **[P0] ثقة عمياء في إجماليات العميل:** `CreateSalesInvoiceDto` يرسل `SubTotal`, `TotalAmount`, `PaidAmount` من العميل، و`CreateAsync` (سطر 188-190) يستخدمها كما هي: `RemainingAmount = dto.TotalAmount - dto.PaidAmount`، دون إعادة الحساب من البنود. **عميل يستطيع إرسال بنود بـ 1000 قطعة و`TotalAmount = 0`** → خصم مخزون حقيقي مقابل إيراد صفري. قارن مع `UpdateAsync` (سطر 382-407) الذي **يعيد الحساب** من البنود — تناقض داخلي في نفس الخدمة، ومع `PurchaseInvoiceService.CreateAsync` (سطر 137-138) الذي يعيد الحساب أيضًا. **الحل:** إعادة الحساب الخادمية في الإنشاء + رفض `PaidAmount` سالب.
2. **[P0] `UnitCost` من العميل:** `SalesInvoiceItemDto.UnitCost` بلا حماية، وهو المستخدم في حساب الأرباح (`DashboardService.cs:63`: `LineTotal - Quantity*UnitCost`) — عميل بصلاحية بيع يمكنه تصفير التكلفة → أرباح وهمية في المؤشرات، أو تضخيمها. التكلفة يجب أن تُستنبط من المنتج/المتوسط المرجّح على الخادم.
3. **[P0] لا آلة حالات للفاتورة:** `UpdateStatusAsync` (سطر 426+) يقبل أي انتقال — بما فيه **مدفوعة (Paid) → مسودة (Draft) رجوعًا** دون أي أثر مالي أو استرجاع مخزون، والعكس؛ `Draft → Paid` مباشرة كذلك. الفاتورة المدفوعة قابلة للتحويل لأي شيء.
4. **[P0] حذف فاتورة مدفوعة مسموح:** `DeleteAsync` (سطر 481+) يسترجع المخزون ويمسح (Soft) الفاتورة — سجل إيراد مدفوع يمكن إخفاؤه بهدوء. لا قيد على `Paid/PartiallyPaid`. قارن مع `PurchaseInvoiceService.UpdateAsync` التي تقفل `Paid` عن التعديل (سطر 174-177) — البيع أكثر تساهلًا من الشراء!
5. **[P1] منطق تعويض هش في `UpdateAsync`:** (سطر 277-378) يعيد الأرصدة القديمة في الذاكرة، يفحص الجديد، وعند فشل يطبق تراجعًا يدويًا ثانيًا (سطر 318-324, 337-346) بنفس الاستعلامات — احتمالات السباق والتشعب عالية، والكود مكرر 4 مرات تقريبًا. نمط أنظف: احسب الفروق (delta) لكل (منتج/باركود) وطبّقها دفعة واحدة.
6. **[P1] RemainingAmount قابل للسالب:** (سطر 413) بلا `Math.Max(0, ...)` بينما المشتريات تقيّده (PurchaseInvoiceService.cs:147, 290) — عدم اتساق يحرف `TotalReceivables` في الداشبورد.
7. **[P2] خطأ كود خاطئ للاستخدام البرمجي:** البند غير الموجود في سطر 172 يعيد `ErrorCodes.InsufficientStock` ("INSUFFICIENT_STOCK") وهو خطأ "باركود غير موجود" — الواجهة الأمامية ستفسر الحالة خطأ.
8. **[P1] حد الائتمان غير مفعل:** `ErrorCodes.CustomerCreditExceeded` معرّف (ErrorCodes.cs:74) و`CreditLimit` يُحفظ على العميل (CustomerService.cs:124)، لكن **لا خدمة تتحقق منه عند البيع الآجل** — البيع بالآجل يتجاوز حد الائتمان بلا رادع.

### 4.4 مرتجعات المبيعات — `SalesReturnService` (278 سطرًا)

- **[P0] لا سقف للكمية المرتجعة مقابل المبيع:** يُتحقق فقط من وجود الفاتورة الأصلية (سطر 126-133) — **لا مقارنة** بين الكميات المرتجعة والكميات المبيعة فعليًا في الفاتورة، ولا منع تكرار المرتجع لنفس البنود. النتيجة: مرتجع بقيمة 1000 قطعة لفاتورة بعت فيها 10 → تضخيم مخزون وهمي (قابل للتحويل لصرف فعلي).
- **[P1] لا مطابقة مستودع الفاتورة الأصلية:** يمكن إرجاع بضاعة صالة X إلى مستودع Y مختلف — استرجاع جغرافي غير متسق مع الأصل.
- **[P1] مرتجع لفاتورة ملغاة/باطلة مسموح:** لا فحص لحالة الفاتورة الأصلية (Cancelled/Voided استُرجع مخزونها أصلًا) → **خصم مزدوج** عند إلغاء الفاتورة ثم مرتجعها.
- **جيد:** التحقق من فروع/عملاء/أرقام متكررة + إعادة البضاعة للنوع الصحيح (Show/Storge) مع إنشاء السجل إن لم يوجد.

### 4.5 فواتير المشتريات — `PurchaseInvoiceService` (493 سطرًا)

- **[P0] منطق المزامنة التفاضلي في `UpdateAsync` معطوب بنيويًا** (سطر 190-264): يقارن بالعدّ فقط ويفترض أن الإضافات في الذيل والحوذف من الذيل (`Skip(newTotalCount).Take(removedCount)`). إذا عدّل المستخدم بندًا وسيطًا أو حذف من الوسط وأضاف آخر — **يُحذف البند الخطأ وتُترك التعديلات دون موازنة مخزون**. إذا عدّل بندًا موجودًا (نفس العدد) — يتغير الإجمالي دون أي تعديل مخزون. لا مفتاح ارتباط (Id) في `UpdatePurchaseInvoiceDto.Items` أصلاً لربط البنود — التصميم لا يدعم التعديل السليم.
- **[P1] متوسط التكلفة غير مرجّح:** `UpdateProductsCostAsync` (سطر 426-445): `AveragePrice = (Average + Price)/2` — متوسط تسلسلي بسيط يتجاهل الكميات؛ شراء 10000 قطعة بسعر 9 وواحدة بسعر 100 يحرّك المتوسط 45.5 بدل ~9. حساب المتوسط المرجّح يتطلب مخزونًا متاحًا (Quantity)، والسطر 433 يستبدل `CostPrice = item.UnitPrice` دائمًا حتى لو كان التعديل عكسًا لمرتجع.
- **[P2] إبطال كائنات الربط بـ null! (سطر 105-107, 294-298):** `invoice.Supplier = null!` إلخ — حيلة لتجنب تعارض التتبع تدل على هشاشة في اختيار الأسلوب (كرّر الاستعلام بعده لإعادة الجلب، أي كلفة إضافية).
- **[P2] كود خطأ خاطئ:** كل أخطاء "فاتورة المشتريات غير موجودة" تعيد `PurchaseOrderNotFound` (سطر 65, 170, 453, 480) — كود الطلبيات في سياق الفاتورة.
- **جيد:** إعادة الحساب من البنود، تفصيل Breakdowns بالنكهات، حماية Paid من التعديل، عكس المخزون عند الإلغاء.

### 4.6 أوامر الشراء — `PurchaseOrderService` (399 سطرًا)

- **[جيد] قفل الحالات:** Received نهائي (منع تكرار الاستلام/تكرار المخزون)، والملغاة لا تُعاد.
- **[P1] توليد رقم فاتورة قابل للتصادم:** `PINV-{yyyyMMddHHmmss}` (سطر 330) — استلام طلبيتين في نفس الثانية = تصادم رقم فاتورة → الفهرس الفريد يرمي 500 (غير ملتقط). أيضًا الفاتورة المولدة تُعتبر `Paid` نقديًا دائمًا (سطر 339-340, 371-372) — افتراض مالي غير قابل للنقض (والتدقيق) من المستخدم.
- **[P2] `UpdateAsync` حذف كامل البنود وإعادة إنشائها** (سطر 176-191) — يدمر الـ Ids ويخسر التتبع؛ مقبول قبل الاستلام، لكن يكشف غياب نمط تعديل تفاضلي موحد في كل خدمات البنود.
- **[P2] N+1:** حلقتان داخليتان لكل بند (جلب المخزون + جلب الباركود للاستنباط).

### 4.7 مرتجعات المشتريات — `PurchaseReturnService` (283 سطرًا)

**الأفضل انضباطًا بين خدمات المخزون:** تحقق كامل (مورد/فرع/مخزن من نوع Storge فقط — قاعدة صريحة برسالة توجيهية ممتازة سطر 124-129، تطابق المورد مع الفاتورة الأصلية، كفاية رصيد لكل نكهة/باركود مع رسالة تحمل الرصيد المتاح).

**الملاحظات:**
- **[P1] تحويلات Decimal→Int قسرية:** `(int)Math.Ceiling(item.Quantity)` (سطر 188, 231) — `Quantity` في البنود decimal بينما `StorgeStock.Quantity` int — التقريب بالسقف قد يرفض مرتجعًا مشروعًا عند الحد (رصيد 10، مرتجع 9.5 → يطلب 10). التعارض النوعي بين نماذج الكميات يستحق قرارًا معماريًا موحدًا.
- **[P2] `DeleteAsync` لا يفحص المخزون قبل الإعادة** — إعادة كمية لمخزن ربما استُهلكت تعطي رصيدًا فوق الحقيقة، لكنه القرار الأقل سوءًا هنا.

### 4.8 المخزون — `StockService` (301 سطرًا)

- **[P1] أثر كتابي في القراءة + انفجار إدخالات:** `GetStorgeStocksByWarehouseAsync` / `GetShowroomStocksByWarehouseAsync` (سطر 52-87, 186-221) يجلبان **كل** الباركودات/المنتجات في النظام ثم ينشئان صفوف رصيد صفرية لكل مفقود **في كل نداء**. مع 5000 صنف × قراءة شبكة → آلاف المقارنات واستعلامات إدراج ممكنة كل مرة يفتح فيها المستخدم الشاشة. هذه مسؤولية تعبئة افتتاحية يجب أن تتم عند إنشاء الصنف/الباركود (كما يفعل `ProductService.CreateAsync` أصلًا) أو خلفية (Background Job)، لا في GET.
- **[P2] الترحيل هنا وحده يُقيَّد** (صفحة ≤100 — سطر 97-99, 231-233) بينما بقية الخدمات لا تقيّد (انظر 4.12).
- **جيد:** `SetStorgeStockAsync` يتحقق من نوع المخزن المناسب قبل الضبط.

### 4.9 التحويلات المخزنية — `StockTransferService` (333 سطرًا)

- **[P0-منطق] فقدان تحديث الملاحظات عند تغيير الحالة:** `UpdateAsync` (سطر 163-168): يكتب `transfer.Notes = dto.Notes` على كيان **غير متتبع** (سطر 153: `FirstOrDefaultAsync`)، ثم إذا اختلف الوضع يفوّض فورًا إلى `UpdateStatusAsync` — **الذي يجلب الكيان من جديد بدون الملاحظات** → الملاحظات تُهدر بصمت. حتى بدون تغيير الحالة، التعديل يمر عبر `Update()` على كيان غير متتبع (انظر 3.4).
- **[جيد] التحقق عند الاستكمال:** كفاية رصيد المصدر قبل التنفيذ + إنشاء رصيد الوجهة عند الغياب + منع تحويل لنفس المخزن + قفل Completed عن التعديل والحذف.
- **[P1] فحص ثم خصم في جولتين منفصلتين** (سطر 202-234 فحص، ثم 237-302 خصم) — نافذة سباق بين الاثنين (راجع القسم 5).

### 4.10 التسويات الجردية — `StockAdjustmentService` (201 سطرًا)

- **[جيد]** توثيق الفرق (`DifferenceQuantity`) ضمن البنود = أثر جرد سليم، والضبط يعيّن الكمية الفعلية مباشرة.
- **[P2]** حذف التسوية لا يعكس أثرها على المخزون — بعد الحذف، الرصيد يبقى "معدّلًا" بينما السجل المبرر اختفى (Soft-Delete فقط) — قرار قابل للنقاش يستحق توثيقًا صريحًا أو منع حذف التسويات المنفذة.
- **[P2]** لا تحقق أن `SystemQuantity` المرسلة تطابق الرصيد الفعلي وقت الجرد — الطرف الذي ينشئ التسوية يحدد "رصيد النظام" بنفسه.

### 4.11 الكتالوج — `ProductService` (433 سطرًا) و`ProductExcelService` (1,645 سطرًا)

**ProductService:**
- **[P1] انفجار توليد الأرصدة الافتتاحية:** `CreateAsync` (سطر 154-248): لكل صالة عرض × منتج، ولكل مخزن × كل باركود — 10 مخازن و5 نكهات = 50 صف رصيد عند كل إنشاء صنف. مع نمو النظام يصبح هذا عبء إدخال غير محدود.
- **[P1] الارتداد الخاطئ عند غياب النوع:** `if (showrooms.Count == 0) showrooms = GetAllAsync()` (سطر 156-159, 198-201) — عند غياب صالة عرض يُنشأ `ShowroomStock` **داخل مخازن Storge** والعكس! خلط أنواع الأرصدة بنيويًا.
- **[جيد]** فحص تكرار الباركود (داخل الطلب وخارجيًا)، هو الوحيد الذي يلتقط `DbUpdateConcurrencyException` (سطر 408-411)، ومزامنة البنود/الباركودات في `UpdateAsync` تفاضلية وسليمة (على العكس من خدمات الفواتير) — يُستحسن نقل هذا النمط إليها.
- **[P2]** `UpdateAsync` يعيّن باركود الربط عبر `ProductBarcode = newBc` (سطر 393) بدل `ProductBarcodeId` — يعمل لكنه يخالف بقية المواضع.

**ProductExcelService:**
- **[P2-صيانة] God Class بمقاييس كل المشروع:** تصدير (شكلان) + قالب + تحقق + استيراد (شكلان) + توليد ملف الصفوف الفاشلة + كشف الأوراق + تسلسل باركودات — سبع مسؤوليات في ملف واحد. تجزئته (Exporter/Importer/Validator/Detector) أولوية صيانة لا وظيفة.
- **[جيد جدًا]** هو النموذج الذهبي للمعاملات: `BeginTransactionAsync` → معالجة → `Commit` مع `Rollback` في الـ catch (سطر 641-660) — النمط الذي يجب استنساخه في كل الخدمات المالية.
- **[P1]** التحميل بالجملة: `GetAllAsync` لسبع مجموعات كاملة في الذاكرة قبل التصدير/التحقق — على قاعدة كبيرة هذا ذروة استهلاك حقيقية (استعلامات تدفقية أو تقسيم دفعات هو البديل).

### 4.12 الخدمات المساندة

**`DashboardService` (367 سطرًا):**
- **[P1] ذاكرة بدل SQL:** `GetSalesTrendInternalAsync` (سطر 204-247) يحمل **كل فواتير N أيام مع بنودها** ثم يجمع أيامها في الذاكرة بـ LINQ-to-Objects — بينما `GroupBy` في SQL (`EF.Functions.DateDiffDay` أو تجميع بالتاريخ) يفعلها على الخادم. نفس الروح في `todayProfit` (سطر 56-64) الذي يجلب فواتير اليوم **كاملةً** ببنودها لحساب رقم واحد قابل للتجميع في SQL.
- **[P1] تضارب UTC/محلي:** الداشبورد يحسب حدود اليوم/الشهر بـ `DateTime.Now` (سطر 34-38) بينما الخدمات تحفظ `DateTime.UtcNow` — على خادم بتوقيت يخالف توقيت العمل، "مبيعات اليوم" تُحسب بنافذة زمنية منحرفة.
- **[P2] حد النقص مُضمّن:** `s.Quantity <= 5` (سطر 301, 309) بجانب `MinStockLevel` — رقم سحري يتجاهل حد الصنف إن كان أعلى.
- **[جيد]** استبعاد الملغاة/الباطلة من كل المؤشرات، وتسمية عربية للطرق/الحالات مباشرة في الطبقة.

**`FileUploadService` (130 سطرًا):**
- **[P1-أمن] تحقق بالامتداد فقط:** `AllowedExtensions = {.jpg,.jpeg,.png,.webp}` (سطر 21) بلا فحص توقيع الملف (Magic Bytes) — ملف HTML/JS بامتداد `.webp` يُخزَّن ويُقدَّم من `wwwroot` = ثغرة XSS مخزنة. التحقق بالمحتوى (`Image.DetectPixelFormat` أو فحص الرأس) + إضافة رؤوس `Content-Disposition/X-Content-Type-Options` عند التقديم.
- **[P1-أمن] `DeleteAsync` بلا تحقق مسار** (سطر 100-102): يحوّل URL لمسار مباشرة؛ استدعاءه بقيمة من العميل (`../../`) = حذف ملفات خارج wwwroot. الاستدعاءان الحاليان ثابتان/من قاعدة البيانات، لكن الدالة نفسها درع بلا حراسة — يلزم تطبيع + التحقق أن المسار الناتج داخل مجلد uploads (`Path.GetFullPath` ومقارنة البادئة).
- **[جيد]** حد 5MB، فصل بالمستأجر، اسم ملف GUID (يمنع تصادم/حقن الأسماء).

**`BranchService` / `CustomerService` / `SupplierService` / `CategoryService`:** CRUD نمطي منضبط: فحوص فرادة أسماء بنطاق المستأجر (بفضل الفلاتر العامة)، منع حذف فرع فيه مستخدمون، `WarehouseService` يمنع حذف مخزن فيه أرصدة. **[P2]** `BranchService.UpdateAsync` يعيد بناء الهواتف حذفًا وإضافة (سطر 100-116) — فقدان Ids وقفة DB إضافية بلا حاجة.

**`TenantService`:** **[P2]** حذف مستأجر بلا تحقق من وجود فروع/مستخدمين/بيانات تحته — يختفي المستأجر من القوائم بينما بياناته تبقى متاحة لأي JWT صادر قبل الحذف (لا آلية تعطيل). `ToggleActiveStatusAsync` لا يُفعَّل في أي مكان أثناء المعالجة (فقط علم منطقي).

---

## 5. تحليل متقاطع: سلامة التزامن (Concurrency)

المشهد الكامل يجمع ثلاث حقائق من فحص DataAccess:

1. **التحقق ثم التعديل (Check-Then-Act) في كل مكان:** كل خدمة مخزون تفعل: `stock = FirstOrDefault(...)` → `if (stock.Quantity < qty) return` → `stock.Quantity -= qty`. طلبان متزامنان لنفس الرصيد يقرآن نفس القيمة، يجتازان الفحص، ويخصمان معًا → **رصيد سالب / بيع فوق المتاح** (Oversell).
2. **RowVersion موجود على كيانات الأرصدة** (`StorgeStockConfiguration.cs`, `ShowroomStockConfiguration.cs` — `IsRowVersion + IsConcurrencyToken`) — أي أن قاعدة البيانات **سترفض** الكتابة المتنافسة.
3. **لكن لا خدمة واحدة من خدمات المخزون تلتقط `DbUpdateConcurrencyException`** — الرفض يتحول إلى 500 خام بلا رسالة عربية ولا إعادة محاولة، بينما `ProductService` وحده يلتقطه.

**النتيجة:** لديكم درع أمانة البيانات (قفل تفاؤلي) **مركّب ومطفأ الاستخدام**. الخيارات: التقاط الاستثناء وإعادة الجلب وإعادة المحاولة في مواضع الأرصدة، أو Select-for-Update عبر معاملة صريحة + `AsTracking` (النمط المعمول به مع SQL Server عند مستوى عزل ReadCommitted مع RowVersion)، أو تفويض الحساب لقاعدة البيانات (`ExecuteUpdate` الشرطي: `UPDATE ... SET Quantity = Quantity - @qty WHERE Quantity >= @qty` والتحقق من RowsAffected) — وهو الأنظف للخصومات المتزامنة.

ضمن نفس السياق: الفهارس الفريدة (`TenantId + InvoiceNumber`, `WarehouseId + Barcode/Product`) هي خط الدفاع الأخير ضد السباق — تعمل، لكن الفشل بها يظهر كـ 500 غير ملتقط بدل رسالة `*_NUMBER_EXISTS` اللطيفة.

---

## 6. سجل المشاكل مصنّفًا بالأولوية

### P0 — يجب إصلاحها قبل أي نشر (صحة بيانات مالية/مخزنية وأمان)

| # | المشكلة | الموقع (ملف:سطر) | الأثر |
|---|---|---|---|
| 1 | سر JWT احتياطي مضمّن بالشيفرة | AuthService.cs:124 | تزوير توكنات مدير عند غياب الإعداد |
| 2 | إجماليات فاتورة المبيعات من العميل بلا إعادة حساب | SalesInvoiceService.cs:188-190 | خصم مخزون مقابل إيراد صفري (تلاعب) |
| 3 | UnitCost من العميل تُغذي حساب الأرباح | SalesInvoiceItemDto + DashboardService.cs:63 | أرباح وهمية في المؤشرات |
| 4 | لا سقف مرتجع المبيع مقابل المبيع + مرتجع لفاتورة ملغاة | SalesReturnService.cs:126-133 | تضخيم مخزون وهمي/خصم مزدوج |
| 5 | منطق المزامنة في تعديل فاتورة المشتريات معطوب (مقارنة بالعدّ والذيل) | PurchaseInvoiceService.cs:190-264 | حذف بنود خاطئة/عدم موازنة مخزون |
| 6 | فقدان الملاحظات + Update غير متتبع | StockTransferService.cs:151-175 | فقدان بيانات صامت |
| 7 | لا معاملات DB في دورة المستندات (بيع/شراء/مرتجعات/تحويل) | كل الجدول في 3.3 | حالات وسيطة مفكوكة عند أي فشل |
| 8 | Check-Then-Act بلا التقاط DbUpdateConcurrencyException | كل خدمات الأرصدة (قسم 5) | Oversell + 500 عشوائي تحت الحمل |
| 9 | حذف/تراجع حالة فاتورة مبيعات مدفوعة بلا قيود | SalesInvoiceService.cs:426-525 | إخفاء إيرادات مدفوعة (ثغرة تدقيق) |

### P1 — مهم (أداء، سلامة مالية ثانوية، أمن تطبيقي)

1. **حد ائتمان العميل غير مفعل** رغم وجود الكود والنموذج (`ErrorCodes.cs:74`).
2. **N+1 منتشر** في حلقات الأرصدة والبنود (Sales/Purchase/Transfer/Returns) وفي `UserService.GetPagedUsersAsync:83-86` — تجميع مفاتيح الرصيد مسبقًا واستعلام `ToDictionary` واحد لكل مجموعة يحل الأغلب.
3. **Distributor Dashboard بالذاكرة** (تحميل فواتير كاملة بدل تجميع SQL) + تضارب `DateTime.Now` مع `UtcNow`.
4. **متوسط تكلفة غير مرجّح بالكميات** (`PurchaseInvoiceService.cs:426-445`).
5. **FileUpload:** تحقق بالمحتوى لا بالامتداد + تحصين `DeleteAsync` ضد اجتياز المسار.
6. **لا قفل محاولات دخول** ولا حماية تخمين + كشف وجود المستخدم في التسجيل.
7. **Logout وهمي** بلا آلية إبطال.
8. **انفجار توليد أرصدة افتتاحية** في ProductService (كل مخزن × كل باركود) + الارتداد لخلط الأنواع عند غياب المخزن المطلوب.
9. **كتابة ضمن القراءة** في StockService (بذر الأرصدة الصفرية عند كل فتح شاشة) وUserService (إنشاء أدوار عند كل قراءة).
10. **أرقام فواتير مولّدة بطابع زمني** قابلة للتصادم (`PurchaseOrderService.cs:330`).
11. **بقايا البيع بلا قيد حالة (آلة حالات)** — انتقالات مثل Paid→Draft.
12. **Overfetching للـ Summary lists** عبر Specifications "بالتفاصيل الكاملة" (3.2).

### P2 — تحسيني (جودة/صيانة)

1. `ProductExcelService` — تجزئة God Class (1,645 سطرًا).
2. أكواد أخطاء خاطئة: `PurchaseOrderNotFound` لفواتير المشتريات، `InsufficientStock` لباركود مفقود.
3. Include بنصوص سحرية `"Items.Product"` بدل تعبيرات مُصرَّفة.
4. التسجيل اليدوي المركزي في DI بلا التحقق من التغطية (+ سطر UserService الشاذ 82).
5. عدم اتساق تقييد الترحيل (StockService يقيد ≤100 وحده).
6. خلط int/decimal في الكميات وتحويلات `(int)Math.Ceiling`.
7. عدم اتساق التعامل مع الهواتف (إعادة بناء) والبنود (HardDelete للكل) بين الخدمات.
8. حذف فعلي للمستخدمين مقابل Soft-Delete عام في النظام.
9. `RemainingAmount` سالب ممكن في المبيعات مقابل تقييد المشتريات.
10. حد النقص الصلب `<= 5` في الداشبورد.
11. حذف التسوية الجردية لا يعكس أثرها.

---

## 7. ملاحظات البنية التحتية المُقتَرَحة كأصول (كان يجب أن تُستثمر)

رصدتُ أصولًا جاهزة في المشروع **لكنها غير مستغلّة** في المواضع الأخطر — استثمارها هو أرخص طريق للإصلاح:

1. **`BeginTransactionAsync/Commit/Rollback` في UnitOfWork** — مستخدمة فقط في 3 خدمات إكسل/صور. البنية جاهزة للتعميم الفوري على دورة المستندات.
2. **RowVersion على جداول الأرصدة والفواتير** — الدفاع ضد السباق موجود على مستوى EF، ينقصه فقط التقاط الاستثناء وإعادة المحاولة في الخدمات.
3. **الفهارس الفريدة المركّبة بالمستأجر** — خط الدفاع ضد تكرار الأرقام موجود؛ ينقصه التقاط `DbUpdateException` وتحويلها إلى رسائل `*_NUMBER_EXISTS`.
4. **`FirstOrDefaultTrackedAsync`** — موجود ويستخدم بشكل صحيح في أماكن؛ يحتاج توحيدًا في الباقي.
5. **نمط الموازنة التفاضلية السليم** موجود مكتملًا في `ProductService.UpdateAsync` (بنود/باركودات) — يمكن استنساخه لخدمات الفواتير بدل منطق الذيل المعطوب.
6. **`ErrorCodes`** — منظومة كاملة (101 كود) تحتاج فقط إلى تصحيح الاستخدامات الخاطئة القليلة.

---

## 8. القابلية للاختبار والصيانة والملاحظات العامة

- **صفر اختبارات آلية للخدمات:** الحل (`RetalSystemAPI.slnx`) لا يحوي مشروع اختبار واحد (مجلد `tests/` فيه فحوص UX لسطح المكتب فقط). أخطر منطق أعمال (أرصدة، فواتير، مرتجعات) **غير مغطى بأي اختبار** — وهو تحديدًا المنطق الذي أثبت هذا التحليل وجود ثغرات فيه. الأولوية: اختبارات تكامل للمعاملات المالية بـ SQLite InMemory أو Testcontainers.
- **صفر تسجيل (Logging):** لا استخدام واحد لـ `ILogger` في كامل طبقة الخدمات. كل فشل صامت من منظور التشغيل (لا أثر من أخذ النسخ الاحتياطي للتحقيق، لا تتبع من قام بماذا). مع أن `AuditInterceptor` يوثّق `CreatedByUserId/UpdatedAt` على البيانات — الإطار موجود للبناء عليه.
- **التوثيق العربي ممتاز:** كل صنف وعضو موثق بـ XML Doc عربي واضح — أصل تعاوني حقيقي يرفع جودة الصيانة.
- **CancellationToken ممتد عبر كل العمليات** — انضباط نادر وممتاز.
- **رسائل الخطأ العربية الغنية بالسياق** (تضمين اسم الصنف والرصيد المتاح، كما في PurchaseReturnService.cs:192-194) — تجربة مستخدم جيدة، تستحق التعميم.

---

## 9. خطة العمل الموصى بها (مرتبة)

**المرحلة 1 — إغلاق P0 قبل أي عرض حي (أسبوع عمل تقديري):**
1. إزالة سر JWT الافتراضي وإجبار الفشل عند غيابه + قفل محاولات الدخول.
2. إعادة حساب إجماليات فاتورة المبيعات من البنود خادميًا + استنباط UnitCost خادميًا + منع PaidAmount سالب.
3. قصر مرتجع المبيع على كميات غير مرتجعة سابقًا من فاتورة أصيلة غير ملغاة وفي نفس مستودعها.
4. إعادة تصميم `UpdatePurchaseInvoiceDto` بمعرفات بنود (`Id`) ومزامنة تفاضلية حقيقية (نسخ نمط ProductService).
5. لف كل عملية مالية/مخزنية بمعاملة صريحة (النمط جاهز في ProductExcelService).
6. التقاط `DbUpdateConcurrencyException` و`DbUpdateException` في الخدمات + إعادة المحاولة/الرسالة المناسبة.
7. آلة حالات للفاتورة (مصفوفة انتقالات مسموحة) + قفل الحذف للمدفوعة.

**المرحلة 2 — P1 (أسبوعان):** تفعيل حد الائتمان، علاج N+1 (تجميع مسبق)، تجميع SQL للداشبورد + توحيد UTC، متوسط مرجّح، تحصين رفع/حذف الملفات، أرقام مستندات بتسلسل آمن، توحيد Tracked/NoTracking، إزالة الكتابة ضمن القراءة.

**المرحلة 3 — P2 (مستمر):** تجزئة ProductExcelService، اختبارات تكامل للدورات المالية، إدخال ILogger، توحيد أنواع الكميات (int أو decimal — قرار واحد)، مهاجرة الـ Includes النصية، تسجيل DI بالتجميع.

---

## 10. الخلاصة

طبقة الخدمات تُظهر **هوية معمارية ناضجة** — وحدات مجالية نظيفة، عقود موحدة (`ServiceResult` + `ErrorCodes`)، عزل مستأجرين محكم عبر الفلاتر العالمية، وتوثيق عربي ممتاز. لكن بينما الهيكل يرتدي بدلة الأنماط الحديثة، **القلب المالي للنظام** — دورة الفواتير والأرصدة — يعمل بمنطق "ثقة بالعميل + تحقق ثم تعديل بلا معاملات أو أقفال"، وبأصول حماية (معاملات، RowVersion، فهارس فريدة) **مبنية في الطبقة السفلى ومهجورة في طبقة الخدمات**.

الطريق إلى الجاهزية ليس إعادة كتابة بل **تعميم ما هو موجود بالفعل**: المعاملات المستخدمة في الإكسل، والتقاط التزامن المستخدم في المنتجات، والموازنة التفاضلية المستخدمة في الباركودات — على بقية الخدمات، مع إغلاق ثغرتي "إجماليات العميل" و"سر JWT" فورًا.
