<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:RetalSystemAPI.MAUI.ViewModels.Catalog"
             xmlns:models="clr-namespace:RetalSystemAPI.Models.DTOs.Catalog.Category;assembly=RetalSystemAPI.Models.DTOs"
             x:Class="RetalSystemAPI.MAUI.Views.Catalog.CategoriesPage"
             x:DataType="vm:CategoriesViewModel"
             Title="تصنيفات المنتجات"
             FlowDirection="RightToLeft"
             BackgroundColor="{StaticResource AppBackground}">

    <Grid RowDefinitions="Auto, *">

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="16,16,16,8">
            <Label Text="إدارة التصنيفات الرئيسية والفرعية للأصناف" Style="{StaticResource Headline}" FontSize="18" />
        </Border>

        <CollectionView Grid.Row="1" ItemsSource="{Binding Categories}" Margin="16,0,16,16">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:CategoryResponseDto">
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                        <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding Name}" FontFamily="CairoBold" FontSize="16" />
                                <Label Text="{Binding Description}" Style="{StaticResource SubHeadline}" FontSize="13" />
                            </VerticalStackLayout>

                            <Button Grid.Column="1" Text="{Binding IsActive, Converter={StaticResource BoolToActiveTextConverter}}" Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CategoriesViewModel}}, Path=ToggleActiveCommand}" CommandParameter="{Binding .}" Style="{StaticResource OutlinedButton}" HeightRequest="36" Padding="12,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
